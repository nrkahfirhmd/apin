# Review & Integration Report — Cycle 3 (personality content + human-in-the-loop device verification)

Generated by `/review` (the `reviewer` subagent) after T1, T2, T3, T4 reached `in-review` in
`tasks/task-graph.md`. T5 remains `blocked` — contingent on Kv executing T2/T3/T4's scripts on
the physical iPhone 17 outside this session, per `planning/engineering-plan.md`'s explicit
"script, not verified result" framing. Not in scope for this review.

## Ground-truth verification performed

- `git status` / `git diff` read directly (not taken on faith from task self-reports).
- `ApinCore/Sources/AI/PersonalityBrief.swift` read in full, diffed against HEAD.
- `cd ApinCore && swift build && swift test` run directly: build clean, **98/98 tests pass**
  (`ApinCorePackageTests.xctest`, 0 failures, 0.314s), including all 7
  `PersonalitySystemInstructionBuilderTests` cases.
- All three new files under `review/manual-verification/` read in full.
- Cross-referenced UI copy/App Intent claims in those three files against the actual source:
  `Apin/Features/Ask/AskView.swift`, `ApinCore/Sources/AppIntents/AskApinIntent.swift`,
  `Apin/AppIntents/AskApinShortcutsProvider.swift`.
- `grep -rn "^import SwiftUI\|^import UIKit" ApinCore/Sources/` → zero matches (ApinCore UI-import
  restriction intact).
- Stray-duplicate sweep: `find . -iname "* [0-9].*"` → zero matches. `ls | grep xcodeproj` →
  **found a second entry, `Apin 2.xcodeproj`** (see Coding standards check below — not blocking,
  but flagged).
- `swiftlint lint --strict` → **0 violations, 0 serious, 84 files.**

## Merge status

| Task | Merged | Conflicts |
|---|---|---|
| T1 | yes | none |
| T2 | yes | none |
| T3 | yes | none |
| T4 | yes | none |

## Conflicts detected

None. Confirmed by `git diff --stat`: T1 touches exactly one file
(`ApinCore/Sources/AI/PersonalityBrief.swift`); T2, T3, T4 each add exactly one new, independent
file under `review/manual-verification/` (`zero-network-check.md`,
`indonesian-fluency-check.md`, `siri-spotlight-latency-spike.md`). No shared file, no shared
interface, no contradictory logic across the four tasks. `review/manual-verification/` contains
exactly these three files and nothing else — matches T2/T3/T4's self-reports.

Two other files show as modified in `git diff --name-only` — `planning/engineering-plan.md` and
`tasks/task-graph.md` — but these are the planning/task-generation artifacts for this cycle
itself, not task-runner output, and are out of scope for a task-conflict check.

## Architecture check

Matches `planning/engineering-plan.md`'s explicit "no new architecture this cycle" framing, and
`memory/adrs/` is untouched (correctly — nothing this cycle warrants a new ADR).

- **T1**: `PersonalityBrief`'s `struct` shape (5 stored properties, memberwise `init`) is
  byte-for-byte unchanged in the diff — only the `.placeholder` static value's content and
  surrounding doc comments changed. `PersonalitySystemInstructionBuilder.swift` was not touched
  (absent from `git diff --name-only`). This matches the plan's "content-only change... do not
  restructure `PersonalitySystemInstructionBuilder.swift`'s building logic or the shape of
  `PersonalityBrief.swift`'s type" constraint exactly.
- **T2–T4**: each is a new, standalone Markdown file under `review/manual-verification/`, no app
  code touched, matching the plan's "Human-in-the-loop tasks produce a script, not a verified
  result" architecture decision verbatim.

## API compatibility

No breaking changes to `memory/apis.md`. None of the four tasks touch a listed
protocol/type (`JournalRepository`, `JournalQuery`, `JournalDigest`,
`CapabilityGateDebugOverride`/`ForcedCapabilityGate`, `AskAndSaveServicing`,
`JournalEntrySaveSideEffect`/`JournalEntryDeleteSideEffect`, `CapabilityGating`). `PersonalityBrief`
itself is not a listed API entry, and its public shape (`init(name:toneDescriptors:styleGuidelines:
guardrails:languagePolicy:)`) is unchanged — no update to `memory/apis.md` is needed or expected
this cycle, consistent with the plan.

## Naming check

No deviations found.

- T1: no new types/files; `.placeholder` static property name intentionally left as-is (correctly
  noted in-file as avoiding a structural rename outside scope) rather than renamed to something
  like `.default`/`.crowArmbrust` — reasonable, avoids an unnecessary ripple into
  `PersonalitySystemInstructionBuilder.swift`'s reference to it.
- T2–T4: filenames (`zero-network-check.md`, `indonesian-fluency-check.md`,
  `siri-spotlight-latency-spike.md`) are lowercase-hyphenated, consistent with the plan's own
  naming of these exact filenames and with the rest of `review/manual-verification/`'s (new)
  convention. No Swift-naming rules apply to these (documentation-only).

## Coding standards check

**T1 — content-only Swift change:**
- `swift build` / `swift test`: clean, 98/98 pass (independently re-run, not taken from T1's
  self-report).
- `swiftlint lint --strict`: 0 violations across 84 files, including `PersonalityBrief.swift`.
- `PersonalitySystemInstructionBuilderTests.swift` is untouched (absent from `git diff
  --name-only`) and its 7 cases all pass — satisfies the acceptance criterion "passes unmodified
  in shape."
- Content check against the plan's Risk framing: the new brief is genuinely tone-anchored, not
  persona simulation. `toneDescriptors`/`styleGuidelines`/`guardrails` reference "witty,"
  "mercenary-rogue swagger," "cocky but likable," "quick with a quip," and explicitly instruct
  "always answer the actual question directly" and "Dial back the swagger for serious, sensitive,
  or safety-relevant topics — read the room," plus a new guardrail, "Wit and charm must never come
  at the cost of a direct, useful answer." No backstory, setting, or character name/lore from
  *Trails of Cold Steel* is encoded — only tone/voice qualities, exactly per the plan's mitigation
  ("keep the brief's guidance behavior-anchored... rather than trying to encode the character's
  full backstory/setting"). The existing `"Admit uncertainty rather than fabricating an answer."`
  guardrail is preserved verbatim.

**T2/T3/T4 — documentation deliverables, held to a precision/completeness bar, not a runtime-
correctness bar (per the plan's explicit "script, not verified result" framing — no code-behavior
testing burden applies to these):**
- All three are self-contained: each opens with "This document is self-contained. You should not
  need to open `planning/engineering-plan.md` or `tasks/task-graph.md` to run it," and independent
  reading confirms this holds — no unresolved forward references, no assumed session context.
- All three have a genuinely blank "Result" section (checked verbatim — no pre-filled dates,
  pass/fail, or notes; each includes an explicit "Leave blank until you've actually run this...
  do not pre-fill or guess" instruction).
- Pass/fail criteria are concrete in all three, not vague:
  - T2: byte/connection-count-based pass/fail for Method A, functional-completion-under-
    zero-connectivity for Method B, explicit scoping of what counts (Apin process only, not
    system daemons).
  - T3: 5 concrete Indonesian questions (factual, personal-data-adjacent factual, conversational,
    long/complex, rephrased-factual) — satisfies the "3–5 questions, different phrasing" acceptance
    criterion — plus itemized pass/fail linguistic criteria (grammar, code-switching, on-topic,
    vocabulary, factual correctness) and an explicit "surface back to Kv, don't auto-shrink scope"
    instruction on fail.
  - T4: a concrete derived go/no-go threshold (10s absolute ceiling, 1.5x baseline-overhead
    tolerance with a documented one-outlier allowance), explicitly and correctly labeled as *not*
    an Apple-sourced figure, with pointers to check for an authoritative figure first. This
    labeling is accurate and appropriately hedged — the task-runner's stated judgment call is
    reasonable and transparently flagged, not silently presented as fact.
- Cross-checked UI/App Intent references against actual source, all accurate:
  - T2/T3 reference the "Ask Apin something…" text field placeholder, the "Ask" button, and the
    "Ask" navigation title — all verified verbatim in `Apin/Features/Ask/AskView.swift` (lines
    107, 115, 126).
  - T4 references `AskApinIntent.swift`'s `supportedModes = .foreground(.immediate)` (verified,
    line 41), `AskView.swift`'s `applyPendingDeepLinkQueryIfNeeded()` (verified, line 96), and the
    two App Shortcut phrases "Ask Apin a question" / "Ask a question in Apin" — verified against
    `AskApinShortcutsProvider.swift`'s `\(.applicationName)`-interpolated phrases (lines 37–38),
    which correctly resolve to those exact strings given the app's display name is "Apin."
  - T2's static-analysis grep claim (`grep -rln "URLSession\|CFNetwork\|import Network"` across
    `Apin/`, `ApinCore/`, `ApinWidget/` returning zero matches) independently re-run below —
    confirmed.
- No app code was touched by T2/T3/T4, matching their scope exactly.

**Repo-wide hygiene sweep (per `memory/coding-standards.md`'s "repo-wide config files are
nobody's task-scope by default" and iCloud stray-duplicate lessons):**
- `find . -iname "* [0-9].*"` — zero matches. No numbered iCloud conflicted-copy files
  (` 2.swift`, ` 2.entitlements`, etc.) from this wave.
- `ls | grep xcodeproj` — **found `Apin 2.xcodeproj` alongside the real `Apin.xcodeproj`.**
  Inspected directly: it contains only `project.xcworkspace/` and `xcuserdata/` — **no
  `project.pbxproj`** — so it is not a duplicated build target/project definition, just stray
  Xcode-generated user-data/workspace scaffolding, and it is `.gitignore`d (`xcuserdata/` pattern;
  confirmed via `git status --porcelain --ignored`, listed as `!!`). It will not be committed and
  does not affect the build (`swift build`/`swift test`/`swiftlint` above all ran clean). However,
  its presence is exactly the class of artifact Sprint 1's incident was about, and a prior commit
  in this repo's history (`1b8147f chore: remove stray duplicate Apin 2.xcodeproj`) already
  removed one instance of this same problem — its reappearance (mtime today) suggests Xcode was
  opened against this iCloud-synced project again and regenerated shadow scaffolding. Not a task
  defect (none of T1–T4 touch Xcode project files) and not a blocker, but flagged for Kv to `rm
  -rf "Apin 2.xcodeproj"` and be aware the underlying cause (iCloud Drive + Xcode) can keep
  recreating it.
- `grep -rln "URLSession\|CFNetwork\|import Network" Apin/ ApinCore/ ApinWidget/` — zero matches,
  confirming T2's cited static-analysis claim is accurate as written.
- `swiftlint lint --strict` — 0 violations, 0 serious, 84 files (up from Sprint 1's 78-file
  baseline, consistent with cycles 2's/3's added files; `PersonalityBrief.swift` linted clean).

## Verdict

**Ready for `/qa`: yes.**

T1–T4 all pass review on independently-verified evidence, not self-report. No conflicts between
the four tasks (they touch four disjoint files). No architecture, API, naming, or coding-standards
deviations found. The one hygiene item (`Apin 2.xcodeproj` stray scaffolding) is gitignored,
doesn't affect any build/test/lint result, and isn't caused by this cycle's tasks — it's a
recommended cleanup for Kv, not a rework item for any task, and does not block `/qa`.

T5 stays `blocked`, unchanged, per the plan's explicit framing that this is an acceptable,
tracked outcome independent of `/qa` readiness — `/qa` should validate T1–T4's deliverables only
and note T5 as carried-forward, not treat it as a review/QA failure.
