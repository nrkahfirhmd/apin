# Review & Integration Report — Apin v1.3 (Cycle 4: language-scope decision + Spotlight invocation diagnosis)

Generated by `/review` (the `reviewer` subagent) after T1, T2, T3, T5 in `tasks/task-graph.md`
reached `in-review`. T4 is out of scope for this review (stays `blocked`, contingent on Kv
executing T3's script and reporting back — not runnable this cycle). Feeds into `/qa`.

## Merge status

| Task | Merged | Conflicts |
|---|---|---|
| T1 | yes | none |
| T2 | yes (findings-only, no code merge needed) | none |
| T3 | yes | none |
| T5 | yes (findings-only, no code merge needed) | none |

Ground-truth-verified via `git diff --stat`, per-file `git diff`, a fresh read of the current
file contents, and an independent `swift test` run in `ApinCore` (not the task-runner's reported
run) — not by trusting completion summaries.

Diff footprint for the whole in-review set (`git diff --stat`):

```
ApinCore/Sources/AI/PersonalityBrief.swift          |   8 +-   (T1)
tasks/task-graph.md                                 | 537 ++--- (T2 + T5 Findings appended, plus /tasks' own regeneration)
```
plus one new untracked file, `review/manual-verification/siri-spotlight-invocation-recheck.md`
(T3). `memory/decisions.md`, `memory/technical-debt.md`, `planning/engineering-plan.md`, and the
three other `review/manual-verification/*.md` files also show as modified in `git status`, but
those are pre-existing changes from before this session (Cycle 3's `/remember` filling in Kv's
device-verification results, and this cycle's `/plan`) — confirmed by their diff content (Kv's
Result-section fill-ins dated 11 August 2026, and the Cycle 3→4 plan rewrite), not attributable to
any of T1/T2/T3/T5. No task this review covers touched them.

## Conflicts detected

None. T1, T2, T3, and T5 touch entirely disjoint surfaces, exactly as `tasks/task-graph.md`
predicted:

- **T1** touched exactly one file, `ApinCore/Sources/AI/PersonalityBrief.swift` (confirmed via
  `git diff --stat` — no other file appears in T1's attributable diff).
- **T2** touched no source file (diagnostic-only, as scoped) — its only footprint is a **Findings**
  subsection appended under its own section in `tasks/task-graph.md`.
- **T3** added exactly one new file, `review/manual-verification/siri-spotlight-invocation-recheck.md`
  (confirmed untracked/new via `git status --porcelain`, not an edit to any existing file).
- **T5** touched no source file (routine re-verification, no discrepancy found) — its only
  footprint is a **Findings** subsection appended under its own section in `tasks/task-graph.md`.

T2 and T5 both append-only into `tasks/task-graph.md`. Read the full current file end-to-end: T2's
"Findings (re-run 2026-08-11, Cycle 4)" subsection sits cleanly under T2's task-detail section and
closes before T3's "### T3 —" header begins; T5's equivalent subsection sits cleanly under T5's
section and closes before the "## Blocked / notes" section begins. No truncation, no
interleaving, no duplicated/garbled headers — the two wave-2 agents' appends merged cleanly.

`siri-spotlight-latency-spike.md` (Kv's Cycle 3 Result record, which T3's own scope explicitly
says must stay untouched) shows a diff, but it is the same pre-existing Cycle-3 fill-in noted
above — dated 11 August 2026, filled in before this cycle's session, not touched by T3. T3 added
its new file alongside it without modifying it.

## Architecture check

Matches `planning/engineering-plan.md` and `memory/adrs/`. No new architecture was introduced this
cycle (none was in scope):

- **T1** is a pure content edit to `PersonalityBrief.placeholder.languagePolicy`, consistent with
  the plan's framing ("a one-field content edit, not a refactor") and with
  `PersonalitySystemInstructionBuilder.build(from:)`'s existing composition logic, which T1 did not
  touch. `PersonalityBrief`'s type shape, `.placeholder`'s name, and the builder's building logic
  are all unchanged (verified by reading the full current file — see Ground-truth verification
  below).
- **T2**'s re-run reuses ADR 001's exact Cycle-1 verification method (build → inspect
  `Metadata.appintents/extract.actionsdata`) rather than introducing a new mechanism, consistent
  with ADR 001's own "Consequences" note that this should be re-verified per-SDK, not assumed
  stable. T2's findings reproduce ADR 001's split (App Shortcuts provider in the `Apin` app
  target, `AskApinIntent` in `ApinCore`) with no drift.
- **T3** produces documentation only, no app code, consistent with the plan's Constraints section.
- **T5** reconfirms the existing `iOS 26.0` / A17-Pro-or-M-series-or-later floor, no
  `project.yml`/`@available` changes, consistent with the established pattern from Cycles 2-3.

## API compatibility

No breaking changes. `memory/apis.md` has zero references to `PersonalityBrief`/`languagePolicy`
(confirmed via `grep`), and none of T1/T2/T3/T5 changed a public type's shape, a protocol's
requirements, or a function signature — T1 changed only a `String` field's *value*, not
`PersonalityBrief`'s structure. Nothing in `memory/apis.md` needs updating as a result of this
cycle's tasks.

## Naming check

No deviations found. T1 did not introduce or rename any symbol. No new types/files were added by
T2 or T5 (findings-only). T3's new file follows the existing `review/manual-verification/*.md`
naming convention (`siri-spotlight-invocation-recheck.md`, alongside
`siri-spotlight-latency-spike.md`, `indonesian-fluency-check.md`, `zero-network-check.md`).

## Coding standards check

- **T1** — clean. Read `ApinCore/Sources/AI/PersonalityBrief.swift` in full:
  - `languagePolicy` now reads `"Always respond in English, regardless of the question's
    language."` — no residual mirroring/Indonesian-language wording anywhere in the file.
  - The header doc comment (lines 18-21) no longer says "carried forward unchanged" or frames
    open question #5 as unresolved; it now reads: *"Open question #5 (mirror-question-language
    product decision) is resolved as of Kv's 2026-08-11 sign-off (see `memory/decisions.md`):
    responses are English-only, with no mirror-language behavior."*
  - No other file in the repo was touched by this task (`git diff --stat` shows exactly one file
    for T1's attributable change).
  - `swift test` in `ApinCore`, run independently in this review (not taken from the task-runner's
    report): all 98 tests pass, including all 7
    `PersonalitySystemInstructionBuilderTests` cases (`testDefaultOutputIncludesLanguageHandlingInstruction`
    included), unmodified — the acceptance criterion that this test needed no edits holds.
- **T2** — clean, diagnostic-only as scoped. No source file touched. Findings subsection is
  internally consistent: it states an actual build was run this cycle (not assumed from ADR 001),
  cites the exact toolchain (Xcode 26.6), states the generic-platform build failed for an
  unrelated reason (no dev team configured) and that the simulator-destination fallback (which
  T2's own scope explicitly allows) succeeded and ran the App Intents metadata phases. Its "clean
  pass" conclusion follows from what it actually reports: `autoShortcuts` populated with both
  phrases, no "No AppShortcuts found" regression for the `Apin` target (the one occurrence for
  `ApinWidgetExtension` is correctly explained as expected/by-design, not a recurrence), no source
  drift in the two intent files, and no entitlement/Info.plist gap relative to what the
  `AppShortcutsProvider` mechanism actually requires. It also correctly re-surfaces the pre-existing
  `Apin 2.xcodeproj`/`Apin 3.xcodeproj` stray-duplicate finding as a backlog candidate rather than
  fixing it inline, per its diagnostic-only scope.
- **T3** — clean. `review/manual-verification/siri-spotlight-invocation-recheck.md` is a new file
  and does not overwrite `siri-spotlight-latency-spike.md` (verified above). It has a distinct
  **Part A** (Siri voice invocation, two phrases plus an "Open Apin" fallback probe, explicit
  pass/fail) and **Part B** (Settings > Siri & Search > Apin toggle re-check, explicit pass/fail,
  with a conditional re-test-after-toggling step) — real, executable criteria, not vague
  instructions. **Part C** cross-references T2's *actual* findings verbatim (build clean,
  `autoShortcuts` populated, no drift, no entitlement gap) rather than a stale placeholder — correct,
  since T2 was `done` before T3 was written this cycle, as the task graph's own wave ordering
  intended. The **Result** section (lines 186-202) is genuinely blank — every field is unfilled,
  no pre-filled or guessed values.
- **T5** — clean. No source file edited (no discrepancy found, correctly matching the "only edit if
  a genuine discrepancy is found" rule T2/T5 share). Findings cite two real, dated, URL-cited
  fetches distinct from the prior 2026-08-11 T9 entry in `memory/decisions.md` (the
  `foundationmodels/systemlanguagemodel.md` markdown-mirror URL and
  `https://www.apple.com/apple-intelligence/`, both explicitly re-fetched this task rather than
  restated). It explicitly cross-checks `project.yml`'s `deploymentTarget: "26.0"` on all 4
  relevant targets and every `@available` annotation in `ApinCore/Sources/AI/` (6 hits via a fresh
  `grep`), stating the result as "consistent — no discrepancy found this cycle."
- **Standing iCloud-Drive stray-duplicate sweep** (`memory/coding-standards.md`): re-run
  independently this review —
  ```
  find . -iname "* [0-9].*" (excluding .build/DerivedData)  → (none besides the two below)
  ls | grep -i xcodeproj → Apin 2.xcodeproj/, Apin 3.xcodeproj/, Apin.xcodeproj/
  ```
  `Apin 2.xcodeproj/` and `Apin 3.xcodeproj/` are still present, still untracked/gitignored
  (`git status --porcelain --ignored`), unchanged from T2's finding — not newly worse, and
  correctly still just a flagged backlog candidate, not something either T2 or this review fixes
  inline. `Apin.xcodeproj/` is the legitimate, single tracked project file.
- Wave/parallelism-cap convention (max 2 `task-runner` agents/wave) was followed per
  `tasks/task-graph.md`'s own suggested wave ordering ({T1, T2}, then {T3, T5}) — consistent with
  the clean, non-conflicting diff footprint observed.

## Verdict

**Ready for `/qa`: yes**, for T1, T2, T3, and T5 — all four should be marked `done`.

- **T1 — pass.** Content-only English-only language policy, header doc comment updated, no scope
  creep, tests pass unmodified (independently re-verified: 98/98 `ApinCore` tests green).
- **T2 — pass.** Genuine re-run (not assumed), findings are detailed and internally consistent,
  correctly diagnostic-only (no code change), backlog candidate (stray `.xcodeproj` duplicates)
  correctly surfaced rather than fixed inline.
- **T3 — pass.** New, precise, self-contained script; does not disturb Kv's existing Cycle 3
  record; correctly incorporates T2's real findings; Result section genuinely blank and ready for
  hand-off to Kv.
- **T5 — pass.** Genuine live re-fetch with cited URLs, correct cross-check against
  `project.yml`/`@available`, no discrepancy, no source edit.

No conflicts between any of the four tasks' changes. No architecture, API-compatibility, naming,
or coding-standards issues found. T4 remains correctly `blocked` (not in this review's scope,
contingent on Kv's device execution and report, which cannot happen inside this session) — this is
an expected, tracked carry-forward per the plan's Definition of Done, not a review failure.
