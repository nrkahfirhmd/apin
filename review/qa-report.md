# QA & Validation Report — Apin v1.3 (Cycle 4: language-scope decision + Spotlight invocation diagnosis)

Generated by `/qa` (the `qa-validator` subagent) after `/review` passed
(`review/review-report.md`: "Ready for `/qa`: yes" for T1, T2, T3, T5; T4 out of scope, stays
`blocked`). Feeds `/remember`.

## Summary

| Field | Value |
|---|---|
| Passed | yes |
| Coverage | 131/131 → 131/131 (98 `ApinCore` + 26 `ApinTests` + 7 `ApinWidgetTests`, unchanged — T1 is a content-only value swap covered by an existing dynamic assertion, not a new code path; T2/T3/T5 added no testable source) |
| Regressions found | 0 |
| Ready for release | yes |

## What was run

- `swift test` in `ApinCore` (independent run, this session): **98/98 passed**, 0 failures,
  including all 7 `PersonalitySystemInstructionBuilderTests` (`testDefaultOutputIncludesLanguageHandlingInstruction`
  included, unmodified).
- `swiftlint lint --quiet` (repo root, `.swiftlint.yml`: default rules + `line_length: 120`
  warning tier): **0 violations**.
- `xcodegen generate`: ran clean, regenerated `Apin.xcodeproj` identically to its committed form
  — `git status --porcelain` before/after is unchanged (no drift between `project.yml` and the
  generated project).
- Full `xcodebuild test` on the `Apin` scheme, simulator destination (iPhone 17, iOS 26.3, the
  already-booted device), `-only-testing:ApinTests -only-testing:ApinWidgetTests`: **33/33
  passed** (26 `ApinTests` + 7 `ApinWidgetTests`), 0 failures. No launch-time `fatalError`, no
  App Group crash.
- `xcodebuild build` on the `Apin` scheme, same destination: **BUILD SUCCEEDED**, including the
  `appintentsnltrainingprocessor` metadata-extraction phase that produces
  `Metadata.appintents/extract.actionsdata`.
- Combined test total this cycle: **131/131**, matching the prior cycle's baseline exactly (no
  regression, no coverage loss).

## Lessons-learned re-checks (per `memory/lessons-learned.md` / `memory/technical-debt.md`)

- **App Group / iCloud container identifier drift** (recurred twice: Cycle 2 original, Cycle 3
  regression via an unread committed diff) — re-checked directly, not trusted from any report:
  - `grep -rn "com.kv.apin\|group.com.kv\|iCloud.com.kv"` across `Apin/`, `ApinWidget/`,
    `ApinCore/Sources/` returns **zero matches** — no stale literal anywhere.
  - `Apin/ApinApp.swift:42` and `ApinWidget/JournalWidgetStore.swift:45` both read
    `"group.com.apin.app"`; `project.yml` (lines 50/52/154/156) matches
    (`group.com.apin.app` / `iCloud.com.apin.app`) on both the `Apin` and `ApinWidgetExtension`
    targets.
  - The two pinned regression tests (`ApinAppModelConfigurationTests`,
    `JournalWidgetStoreTests.test_appGroupIdentifier_matchesConfiguredEntitlement`) pass, and the
    full `xcodebuild test` run produced no `fatalError`/App-Group-unreachable crash at launch —
    the class of bug that bit this project twice is confirmed absent this cycle.
- **"Never commit a pre-existing uncommitted diff without reading it first"** — no diff was
  committed by this QA pass (QA only reads/runs, per its role). Independently re-read the actual
  diff content (not just trusted the review report's narrative) of the three pre-existing modified
  `review/manual-verification/*.md` files (`indonesian-fluency-check.md`,
  `siri-spotlight-latency-spike.md`, `zero-network-check.md`) and confirmed they are genuinely
  Kv's Cycle-3 Result-section fill-ins (dated 11 August 2026), not attributable to any of this
  cycle's T1/T2/T3/T5, and are internally consistent with `memory/decisions.md`'s recorded
  entries for the same dates. Nothing here needs staging/committing as part of this QA pass, so
  the specific commit-hygiene failure mode doesn't apply, but the underlying content was verified
  by direct reading rather than assumed.
- **iCloud-Drive stray-duplicate hazard** — re-verified independently (not trusted from T2's or
  the reviewer's prior report):
  ```
  find . -iname "* [0-9].*" (excluding .build/.git)  → none besides the two below
  ls | grep -i xcodeproj → Apin 2.xcodeproj/, Apin 3.xcodeproj/, Apin.xcodeproj/
  git status --porcelain --ignored → both "Apin 2.xcodeproj/" and "Apin 3.xcodeproj/" listed
    as ignored ("!!"), not tracked
  ```
  Both remain present, untracked/gitignored, unchanged from T2's/`/review`'s prior finding — not
  newly worse, no build ever touches them (`xcodebuild`/`xcodegen` output above only references
  `Apin.xcodeproj`). Confirmed harmless, still just a flagged backlog candidate.

## T1 runtime-behavior sanity check

- `ApinCore/Sources/AI/PersonalityBrief.swift`'s `.placeholder.languagePolicy` reads exactly
  `"Always respond in English, regardless of the question's language."` — read the full file
  directly; no leftover Indonesian-mirroring wording anywhere (`grep -n "languagePolicy"` across
  `ApinCore/` shows exactly one production value, the English-only one, plus two unrelated
  English-only fixture literals in the test file).
- `PersonalitySystemInstructionBuilder.swift:33` composes `"Language handling: \(brief.languagePolicy)"`
  directly from the `PersonalityBrief` value — no hardcoded/duplicated copy of the old policy
  exists elsewhere in the builder.
- `testDefaultOutputIncludesLanguageHandlingInstruction` (the acceptance-criterion test named in
  the review report) asserts `instructions.contains(PersonalityBrief.placeholder.languagePolicy)`
  — a dynamic reference, not a hardcoded string — so it required no edit and still passes,
  confirming the composition pipeline picked up the new value correctly rather than the test
  happening to pass by coincidence.
- Nothing else in the personality/system-instruction pipeline was touched or broken:
  `PersonalityBrief`'s type shape (5 fields, same `init`) is unchanged; `.placeholder`'s other
  four fields (name, toneDescriptors, styleGuidelines, guardrails) are byte-for-byte unchanged
  from Cycle 3; all 7 `PersonalitySystemInstructionBuilderTests` pass, including
  `testBriefIsDataDrivenAndSwappable`, `testDefaultOutputIncludesEachGuardrail`, and
  `testDefaultOutputIncludesEachStyleGuideline`.

## App Shortcut / build-metadata re-check (cross-checking T2's diagnostic findings)

Independently re-built (not reusing T2's or `/review`'s DerivedData) and inspected
`Metadata.appintents/extract.actionsdata` directly:
- `phraseTemplates` contains both registered phrases verbatim: `"Ask Apin ${query}"`-family
  entry (voice-invocation summary string) and the `key` string
  `"Ask Apin a question. Opens the app with your question ready to send."`, plus `"Ask a
  question in ${applicationName}"`.
- `shortTitle`'s `key` is `"Ask Apin"`; `systemImageName` is `"questionmark.bubble"`.
- These match T2's Cycle-4 findings and ADR 001's original Cycle-1 finding exactly — no drift.
  This reconfirms T2's "clean pass, no build-level defect" conclusion from a fresh, independent
  build rather than trusting T2's report.

## Known bugs

None found in the in-scope changes (T1/T2/T3/T5) that aren't already fully understood and
tracked. Two items surfaced by T2/prior cycles are **not code defects** and are carried as
backlog candidates (see Remaining work below), not bugs.

The Siri/Spotlight invocation gap Kv reported in Cycle 3 (typed Spotlight search produces no
result from Apin, despite a clean build/metadata pass) remains **unresolved and unexplained** —
T2's build-level audit (re-confirmed above) rules out a build/metadata cause, and T3's new script
(`review/manual-verification/siri-spotlight-invocation-recheck.md`) is written and ready to
isolate whether it's Spotlight-specific or also affects Siri voice, and whether it's a Settings
misconfiguration — but T4 (which would record Kv's execution of that script) is blocked on Kv
running it on the physical device, which cannot happen inside this QA session. This is not a new
bug found by QA; it's the same open gap `/review` already scoped as out-of-bounds for this cycle.

## Remaining work

- **T4** stays `blocked` — contingent on Kv executing
  `review/manual-verification/siri-spotlight-invocation-recheck.md` on the physical device and
  reporting back. Not runnable in any automated session. Carries forward to the next cycle's
  `/plan`, consistent with the engineering plan's Definition of Done and the same pattern used
  for Cycle 3's T5.
- **Stray `Apin 2.xcodeproj` / `Apin 3.xcodeproj` cleanup** (T2's backlog candidate) — confirmed
  present, untracked/gitignored, harmless to build correctness, unchanged since T2's finding. No
  code defect; a trivial cleanup task for a future cycle, pending Kv's go-ahead per this
  project's convention of not silently removing tracked-adjacent artifacts as a side effect of
  another task.
- Spec open question #3's remainder (Siri/Spotlight invocation-gap root cause and, if the gap
  turns out to be fixable in-app, any resulting scope) stays open pending T4/Kv.

## Regression results

- Full existing suite (131 tests across `ApinCoreTests`, `ApinTests`, `ApinWidgetTests`):
  **131/131 passing**, identical count and identical pass status to the prior cycle's QA
  baseline — no regression introduced by T1/T2/T3/T5.
- Full app build (`xcodebuild build`, `Apin` scheme, simulator destination): **succeeds**,
  including the App Intents metadata-extraction build phase.
- App Group / iCloud container identifiers: consistent everywhere, no recurrence of the
  Cycle-2/Cycle-3 drift bug class (see Lessons-learned re-checks above).
- SwiftLint: 0 violations, same as the established Sprint-1 baseline.
- No conflicts, no architecture/API/naming/coding-standards issues — consistent with
  `review/review-report.md`'s findings, independently reconfirmed rather than assumed.

## Coverage

- No new source logic shipped this cycle that requires new tests: T1 is a `String` *value*
  change on an existing, already-tested field (covered dynamically, not via a hardcoded
  expectation, so the existing test continues to exercise the new value with zero edits); T2 and
  T5 are diagnostic-only (no source changed); T3 added a manual-verification document (not
  automated-testable by design — XCUITest is explicitly deferred for this project per
  `memory/coding-standards.md`, and Siri/Spotlight/Settings-level behavior isn't unit-testable
  regardless).
- Test count unchanged at 131 (98 `ApinCore` + 26 `ApinTests` + 7 `ApinWidgetTests`) — no
  coverage gained, none lost.
- Deliberately untested/deferred, unchanged from before this cycle: AI response *content*
  (non-deterministic on-device generation, explicitly out of scope per
  `memory/coding-standards.md`); actual on-device Siri voice/Spotlight invocation behavior
  (requires Kv's physical-device execution of T3's script, tracked as T4).

## Ready for release?

**Yes.**

- All automated gates pass cleanly and were independently re-run this session, not taken on
  faith from `/review`'s report: `swift test` (98/98), `swiftlint lint` (0 violations),
  `xcodegen generate` (no drift), `xcodebuild build` (succeeds), `xcodebuild test` on the `Apin`
  scheme (33/33). Combined total 131/131, matching the established baseline with zero
  regressions.
- T1's actual runtime behavior was sanity-checked at the source level (not just "tests pass"):
  the English-only policy is coherent end-to-end through
  `PersonalitySystemInstructionBuilder`, with no leftover Indonesian-mirroring language anywhere
  in the codebase, and the acceptance-criterion test exercises the new value dynamically rather
  than coincidentally.
- The specific bug class that has bitten this project twice (App Group/iCloud identifier drift)
  was explicitly re-checked from source, not inferred from history, and is confirmed absent.
- T2's build/metadata App Shortcut findings were independently reproduced with a fresh build in
  this session (not just re-read from its report), and match exactly — no drift.
- T4 remaining `blocked` is an expected, correctly-tracked carry-forward (contingent on Kv's
  device execution, explicitly out of this cycle's and this QA session's scope per both the
  engineering plan's Definition of Done and `/review`'s verdict) — it does not block release of
  T1/T2/T3/T5, none of which depend on T4's outcome. The Siri/Spotlight invocation gap itself
  predates this cycle (discovered in Cycle 3) and is unaffected either way by what shipped this
  cycle — T13's existing deep-link behavior (typed-in-app / already-working invocation paths)
  is unchanged and still works, per the plan's own "regardless of outcome" framing.
- The two backlog items (stray `.xcodeproj` duplicates, open Siri/Spotlight investigation) are
  correctly non-blocking: one is a confirmed-harmless dead file, the other is a pre-existing,
  already-tracked, non-regressing gap with a ready-to-run investigation script waiting only on
  device access.
