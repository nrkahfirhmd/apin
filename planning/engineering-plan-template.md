# Engineering Plan — <cycle name>

Generated by `/plan` (the `planner` subagent) from `planning/spec.md` and `memory/`.
This is the input to `/tasks`, which turns it into `tasks/task-graph.md`.

## Sprint backlog

Ordered list of work items for this cycle, each traceable to a requirement in the spec.

| # | Item | Spec ref | Priority |
|---|---|---|---|
| 1 | | | Must |

## Architecture decisions

Decisions made to satisfy the spec's requirements/constraints. Anything with lasting impact
gets promoted to an ADR in `memory/adrs/` by `/remember`.

- <decision> — rationale

## Constraints

Carried over from the spec, plus any the planner surfaced (from `memory/technical-debt.md`,
existing `memory/apis.md` / `memory/database-schema.md`, etc.).

## Definition of Done

Applies to every task in this cycle unless a task overrides it explicitly.

- [ ] Meets the requirement(s) it maps to
- [ ] Passes existing tests; new tests added for new behavior
- [ ] Follows `memory/coding-standards.md`
- [ ] No new entries in `memory/technical-debt.md` without sign-off
- [ ] Reviewed via `/review` and validated via `/qa`

## Risks

What could derail this cycle and the mitigation, if any.
