// AIScaffold.swift
// ApinCore
//
// Scaffold placeholder for the AI module. This directory will hold the capability
// gate service (T2), the FoundationModels session wrapper (T4), and the personality
// system-instruction builder (T5). Intentionally contains no logic — see
// tasks/task-graph.md T1 for scope.
//
// ApinCore must never import SwiftUI/UIKit; keep this module reusable from the
// widget and App Intents extensions.
