# Task Graph — <cycle name>

Generated by `/tasks` (the `planner` subagent) from `planning/engineering-plan.md`.
Each task is implemented independently by `/implement <task-id>` (the `task-runner` subagent).
Tasks with no unfinished dependency can run in parallel.

## Tasks

| ID | Title | Depends on | Status | Owner agent |
|---|---|---|---|---|
| T1 | | — | pending | task-runner |
| T2 | | T1 | pending | task-runner |
| T3 | | — | pending | task-runner |

Status values: `pending → in-progress → in-review → done` (or `blocked` with a reason noted below).

## Task details

### T1 — <title>
- **Backlog item:** links to `planning/engineering-plan.md` item #
- **Scope:** what this task does and does not touch
- **Dependencies:** none / T-ids
- **Acceptance criteria:** how `/review` and `/qa` will judge this done
- **Notes:** anything the task-runner needs that isn't obvious from the spec/plan

<!-- Repeat one ### section per task -->

## Blocked / notes

<!-- Anything that stalled a task and why -->
