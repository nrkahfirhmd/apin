# Task Graph — Apin v1.1 (Cycle 2: hardening + open-decision closure)

Generated by `/tasks` (the `planner` subagent) from `planning/engineering-plan.md`.
Each task is implemented independently by `/implement <task-id>` (the `task-runner` subagent).
Tasks with no unfinished dependency can run in parallel.

**Cycle 2 context:** Kv has confirmed physical Apple Intelligence-capable hardware (iPhone 17)
AND an active Apple Developer account are available this cycle, so T4, T5, and T10 (mapping to
backlog items 4, 5, 10) are no longer blocked on availability the way the plan anticipated.
However, actually connecting to and driving the physical device is an **execution-environment
concern**, not a task-scoping concern — whichever session runs `/implement` on T4/T5/T10 needs
device-connected Xcode/Instruments access (may require the human directly, or a device-attached
session). This is flagged in each of those tasks' Notes; it does not block writing or queuing the
task itself.

## Tasks

| ID | Title | Depends on | Status | Owner agent |
|---|---|---|---|---|
| T1 | Fix `JournalEntry.id` multi-match handling + dedup pass | — | done | task-runner |
| T2 | Add `UIBackgroundModes: [remote-notification]` to `project.yml` | — | done | task-runner |
| T3 | Debug-only capability-gate override + manual verification of all 5 cases | — | done | task-runner |
| T4 | Dynamic zero-network-requests verification (Instruments / Network Link Conditioner) | — | pending | task-runner |
| T5 | Indonesian answer-language fluency verification on real hardware | — | pending | task-runner |
| T6 | Personality brief content swap (pending Kv's brief) | — | done | task-runner |
| T7 | Retention policy confirmation with Kv | — | done | task-runner |
| T8 | T13 auto-submit-on-Spotlight-deep-link UX confirmation with Kv | — | done | task-runner |
| T9 | Re-spot-check minimum iOS/device requirement | — | done | task-runner |
| T10 | Inline Siri/Spotlight latency spike (go/no-go) | — | pending | task-runner |
| T11 | Tagging: manual tag entry/edit + journal search tag filter | — | done | task-runner |
| T12 | Weekly digest / streak view | — | done | task-runner |

Status values: `pending → in-progress → in-review → done` (or `blocked` with a reason noted below).

All twelve tasks are dependency-free with respect to each other — none shares a file/interface
edit with another closely enough to require sequencing. See each task's **Dependencies** field
for the explicit "none" confirmation and the reasoning where it's not obvious (e.g. T6 vs. T3
both touch `AskViewModel`-adjacent code but through disjoint files). All twelve can run in
parallel, subject to the iCloud-Drive parallelism cap in `memory/coding-standards.md` (max 2
`task-runner` agents per wave in this repo) and to T4/T5/T10's execution-environment constraint
(device-connected session required — see their Notes).

Suggested wave ordering (not a dependency requirement, just throughput — smallest/lowest-risk
first per the plan's own "attempt hardware-independent work first" guidance):
1. T1, T2 (trivial, hardware-independent, Must-priority debt fixes)
2. T3 (Must-priority debt fix, hardware-independent but larger)
3. T9 (small Should-priority research task)
4. T6, T7, T8 (decision-confirmation tasks — output depends on whether Kv has answered)
5. T4, T5, T10 (device-dependent — sequence last so their environment constraint blocks the
   minimum possible amount of the cycle, per the plan's explicit risk framing)
6. T11, T12 (Nice-to-have, lowest priority — pull forward only if 1–10 land with room to spare)

## Task details

### T1 — Fix `JournalEntry.id` multi-match handling + dedup pass
- **Backlog item:** #1 (`planning/engineering-plan.md`) / `memory/technical-debt.md` "No DB-level
  uniqueness guarantee on `JournalEntry.id` post-CloudKit"
- **Scope:**
  - `ApinCore/Sources/Persistence/SwiftDataJournalRepository.swift`: `fetch(by:)` (currently
    `try modelContext.fetch(descriptor).first`, line 84) and `delete(id:)` (currently
    `guard let entry = try fetch(by: id) else { return }`, line 96) must stop silently taking
    `.first`/silently deleting one of several matches. Implement a deliberate, documented
    behavior for the multi-match case — the plan leaves the exact strategy to task-runner's
    judgment (e.g. reconcile by `createdAt` recency, or fetch/delete all matches, or log +
    surface via a typed result) as long as it is deliberate and tested, not an implicit `.first`.
  - Add a startup or periodic dedup pass (a new method on `SwiftDataJournalRepository` or a
    small new type in `ApinCore/Sources/Persistence/`) that removes extra entries sharing an
    `id`, for entries that already diverged before this fix landed (e.g. from a hypothetical
    prior CloudKit merge). Wire a call site for it (e.g. app launch in `Apin/ApinApp.swift`) if
    that's the chosen trigger — keep the wiring minimal, this is a defensive pass, not a new
    feature surface.
  - Does **not** touch `@Attribute(.unique)` (deliberately removed in T18/cycle 1, not being
    reopened — see `memory/database-schema.md` and the plan's architecture-decisions section)
    or any other repository method (`save`, `fetch(matching:)`).
  - Add synthetic-duplicate unit tests in
    `ApinCore/Tests/ApinCoreTests/` (mirroring `SwiftDataJournalRepository`'s existing test file
    naming) — since no current code path produces a real duplicate, construct the scenario
    directly: insert two `JournalEntry` instances sharing an `id` into a test `ModelContainer`,
    then assert `fetch(by:)`/`delete(id:)`/the dedup pass behave as documented.
- **Dependencies:** none.
- **Acceptance criteria:**
  - `fetch(by:)` and `delete(id:)` no longer contain an undocumented implicit `.first` — the
    chosen multi-match behavior is explained in a doc comment at the call site.
  - A dedup pass exists, is unit-tested, and is reachable from a real call site (not dead code).
  - New unit tests cover: no duplicates (existing behavior unchanged), 2+ entries sharing an
    `id` on `fetch(by:)`, 2+ entries sharing an `id` on `delete(id:)`, and the dedup pass itself.
  - All existing `ApinCoreTests` pass unmodified in shape.
  - `memory/technical-debt.md`'s "No DB-level uniqueness guarantee..." entry is updated to
    `resolved` (or, if only partially addressed, re-confirmed open with a dated note explaining
    the gap) as part of this task's completion notes for `/remember` to pick up.
  - SwiftLint: 0 new violations.
- **Notes:** No real hardware needed — fully verifiable via the in-memory `ModelContainer` test
  pattern already used elsewhere in `ApinCoreTests`. Independent of T2–T3 (different files); does
  not depend on and is not depended on by any hardware-gated task (T4, T5, T10).

### T2 — Add `UIBackgroundModes: [remote-notification]` to `project.yml`
- **Backlog item:** #2 (`planning/engineering-plan.md`) / `memory/technical-debt.md` "Missing
  `UIBackgroundModes: [remote-notification]`"
- **Scope:** `project.yml`, `Apin` target's `settings.base` block (around line 55–70) — add
  `INFOPLIST_KEY_UIBackgroundModes: [remote-notification]` (or the equivalent XcodeGen array
  syntax already used by neighboring `INFOPLIST_KEY_*` entries in that block). Regenerate the
  Xcode project (`xcodegen generate` or repo's documented equivalent) so the change actually
  takes effect, and confirm the generated `Info.plist`/build settings reflect it. No other file
  should need to change — this is a one-line settings addition, not a code change.
- **Dependencies:** none.
- **Acceptance criteria:**
  - `project.yml`'s `Apin` target has `INFOPLIST_KEY_UIBackgroundModes: [remote-notification]`.
  - Project regenerates cleanly and builds.
  - `memory/technical-debt.md`'s "Missing `UIBackgroundModes: [remote-notification]`" entry is
    updated to `resolved` in this task's completion notes for `/remember`.
- **Notes:** Trivial, hardware-independent. Deliberately kept as its own task rather than bundled
  with T1 — different file (`project.yml` vs. Swift source under `ApinCore/`), different
  acceptance criteria (build-settings verification vs. unit tests), and no dependency between
  them either direction. QA previously reconfirmed this gap via a runtime CloudKit diagnostic
  message ("CloudKit push notifications require the 'remote-notification' background mode") —
  re-running that same check post-fix is a reasonable self-verification step.

### T3 — Debug-only capability-gate override + manual verification of all 5 cases
- **Backlog item:** #3 (`planning/engineering-plan.md`) / `memory/technical-debt.md`
  "Unsupported-device/OS path only structurally verified, never manually exercised" / Req 7
- **Scope:**
  - Add a `#if DEBUG`-gated override mechanism that can force any of the 5
    `CapabilityGateResult` cases (`available`, `unsupportedDevice`, `unsupportedOS`,
    `appleIntelligenceDisabled`, `modelNotReady`) via a launch-environment variable (e.g.
    `ProcessInfo.processInfo.environment["APIN_DEBUG_CAPABILITY_OVERRIDE"]`), likely as a new
    small type or extension near `ApinCore/Sources/AI/CapabilityGate.swift`/
    `CapabilityGateResult.swift`, or a wrapping `CapabilityGating` conformance.
  - Wire it through `Apin/Features/Ask/AskViewModel.swift`'s `AskViewModel.live(...)` factory
    (currently constructs `capabilityGate: CapabilityGate.live()` unconditionally at line 152) —
    under `#if DEBUG`, check the override first and fall back to `CapabilityGate.live()`
    otherwise.
  - Document the gate in-file (why it's `#if DEBUG`, that it's compiled out of Release) per the
    plan's explicit architecture decision that `/review` must be able to verify the gate is real,
    not cosmetic.
  - Perform the manual verification pass this task also owns: actually render
    `Apin/Features/Ask/CapabilityUnavailableView.swift` / exercise `AskView`'s unsupported branch
    end-to-end for all 5 cases in Simulator, using the override to force each case in turn.
    Record what was observed (screenshot description or explicit written confirmation per case)
    in the task's completion notes — this is part of the same task, not a follow-up, since one
    task-runner both builds and self-verifies the override per the plan's guidance.
  - Does not touch `CapabilityGate.checkCapability()`'s production logic itself — only adds an
    override layer in front of it, active only in DEBUG builds.
- **Dependencies:** none.
- **Acceptance criteria:**
  - Override exists, is `#if DEBUG`-gated, and is verifiably absent/inert in a Release
    configuration build (e.g. build with `-configuration Release` and confirm
    `CapabilityGate.live()` is reached unconditionally, or grep the compiled behavior path).
  - All 5 `CapabilityGateResult` cases have been manually rendered end-to-end in Simulator and
    each is confirmed to show correct, non-crashing UI — documented in completion notes.
  - `memory/technical-debt.md`'s "Unsupported-device/OS path only structurally verified..." entry
    is updated to `resolved` in completion notes for `/remember`.
  - No regression to existing `CapabilityGateTests`, `AskViewModelTests`,
    `CapabilityStatusCopyProviderTests`.
  - SwiftLint: 0 new violations.
- **Notes:** Simulator is sufficient — explicitly marked "No" for hardware requirement in the
  plan. Independent of T1/T2 (different files: `ApinCore/Sources/AI/` +
  `Apin/Features/Ask/AskViewModel.swift`, not `Persistence/` or `project.yml`). Independent of
  T4/T5/T10 (this task's coverage is Simulator-only by design, per the plan's explicit
  architecture decision that this unblocks Req 7's QA without needing real disqualifying
  hardware).

### T4 — Dynamic zero-network-requests verification
- **Backlog item:** #4 (`planning/engineering-plan.md`) / `memory/technical-debt.md` "'Zero
  network requests when producing an answer' not dynamically verified" / Req 1 (Must,
  safety-critical)
- **Scope:** Using Xcode Instruments' Network instrument (or Network Link Conditioner set to
  "100% Loss") against the real Ask flow (`Apin/Features/Ask/AskView.swift` →
  `AskViewModel.submit()` → `AskAndSaveService` → `AssistantSessionService`), capture live
  traffic during at least one full model-invocation cycle (ask a question, receive a streamed
  answer) and confirm zero network traffic is generated by the app process during that window.
  This replaces/supplements the existing static-analysis grep argument documented in
  `memory/technical-debt.md` — it does not remove the grep argument, it adds the dynamic
  confirmation on top of it. No code changes are expected unless the capture surfaces an
  unexpected network call, in which case scope expands to identifying and reporting (not
  necessarily fixing within this task) the source.
- **Dependencies:** none.
- **Acceptance criteria:**
  - A documented Instruments/Network Link Conditioner capture exists (trace file, screenshot, or
    written observation log) covering at least one full ask-flow invocation with zero observed
    network traffic from the app process.
  - If traffic *is* observed: documented as a new, clearly flagged finding (not silently ignored
    or silently fixed without review) — this would be a safety-critical regression requiring
    immediate escalation, not a routine task note.
  - `memory/technical-debt.md`'s "'Zero network requests...' not dynamically verified" entry is
    updated to `resolved` with the verification method and date, or re-confirmed still-open with
    a dated "attempted, blocked" note if device/Instruments access could not actually be
    exercised despite being nominally available.
- **Notes:** **Requires physical Apple Intelligence-capable device + Xcode Instruments access.**
  Kv has confirmed an iPhone 17 and active Apple Developer account are available this cycle, so
  this is no longer blocked on availability per se — but connecting to and driving the physical
  device is an execution-environment concern for whoever runs `/implement T4`: this likely
  requires the human directly, or a task-runner session with an actual device-paired Xcode
  connection, neither of which is guaranteed to be the default `/implement` execution context.
  Flag this explicitly when queuing the task rather than assuming any session can pick it up.
  Sequenced independently of T1–T3 (no shared files) and independently of T5/T10 (each verifies a
  different requirement, no shared code path beyond both exercising the same Ask flow — running
  them in the same physical-device session back-to-back is a reasonable efficiency but not a
  dependency).

### T5 — Indonesian answer-language fluency verification on real hardware
- **Backlog item:** #5 (`planning/engineering-plan.md`) / spec open question #5, Req 1 & 2
- **Scope:** On the physical Apple Intelligence-capable device (with the Indonesian language pack
  enabled), manually exercise `AskView`'s mirror-language path (`AskViewModel.submit()` →
  `AskAndSaveService` → `AssistantSessionService`, instructions from
  `ApinCore/Sources/AI/PersonalitySystemInstructionBuilder.swift`) with a representative set of
  real Indonesian questions. Assess whether the on-device model's Indonesian output is fluent and
  usable. Two possible outcomes, both valid task completions:
  - Fluent/usable: update `memory/decisions.md`'s existing "Answer-language mirroring... still
    runtime-unverified" entry to confirmed, no code change needed.
  - Not fluent/usable: per the plan's own risk mitigation, shrink scope to English-only (this
    would mean a follow-up code change to the mirror-language logic — scope that as a new
    finding/recommendation in completion notes rather than silently implementing it within this
    verification task, since it's a product-scope change that should be surfaced back to Kv
    first, consistent with how items 6/7/8 are handled).
- **Dependencies:** none.
- **Acceptance criteria:**
  - A documented set of real Indonesian questions and the model's actual answers (or a written
    fluency assessment if verbatim capture isn't practical) exists in completion notes.
  - `memory/decisions.md` gets a new dated entry recording the verification outcome (confirmed
    fluent, or flagged as needing English-only scope reduction) — not left as a second
    unresolved carry-forward without at least an attempt logged.
  - If not fluent: the finding is surfaced explicitly as needing Kv's decision on scope
    reduction, not silently acted on.
- **Notes:** **Requires physical hardware with Apple Intelligence + Indonesian language pack
  enabled.** Same execution-environment caveat as T4 — flag when queuing that this needs a
  device-connected session, likely the human directly. Independent of T4/T10 (different
  verification target) though may be practically bundled into the same physical-device session
  for efficiency. Independent of T6 (personality brief content) — T6 changes the *brief content*,
  this task verifies *language fluency*; they touch different files
  (`PersonalitySystemInstructionBuilder.swift`'s brief data vs. no code change expected here) and
  neither blocks the other.

### T6 — Personality brief content swap
- **Backlog item:** #6 (`planning/engineering-plan.md`) / spec open question #2
- **Scope:** `ApinCore/Sources/AI/PersonalityBrief.swift` (the swappable brief data T5/cycle-1
  built for exactly this purpose) — if Kv has supplied a real brief (name origin, tone/character
  reference) by the time this task runs, replace the placeholder content there as a **content-only
  change**: no new types, no restructuring, no change to
  `PersonalitySystemInstructionBuilder.swift`'s building logic. Existing structural unit tests in
  `ApinCore/Tests/ApinCoreTests/PersonalitySystemInstructionBuilderTests.swift` must still pass
  unmodified in shape (they test structure, not the placeholder wording, per
  `memory/coding-standards.md`'s testing strategy excluding AI-adjacent content from automated
  assertions).
  If Kv has **not** responded by the time this task runs, the task's output is confirming/
  re-flagging `memory/decisions.md`'s "Personality brief for 'Apin' remains a placeholder..."
  entry as still-open, with no code change — this is the expected, acceptable outcome per the
  plan's own framing ("purely content-blocked... there is no design work left, only waiting on
  the actual brief text").
- **Dependencies:** none.
- **Acceptance criteria:**
  - If brief supplied: `PersonalityBrief.swift` content updated, all existing structural tests
    pass unmodified, `memory/decisions.md` gets a new entry confirming the brief.
  - If brief not supplied: no code change; `memory/decisions.md`'s existing entry is
    re-confirmed/re-flagged as still open in this task's completion notes, and the task is not
    treated as a failure for having no code diff.
- **Notes:** No hardware needed. Blocked on Kv's actual input, not on any other task — check
  wherever Kv's answer would surface (e.g. a message in the orchestrating session, a note left in
  `planning/spec.md` or a scratch file) before defaulting to the "still open" outcome. No
  dependency on T3 despite both eventually touching `Apin/Features/Ask/`-adjacent AI plumbing —
  T3 touches `CapabilityGate`/`AskViewModel`'s capability wiring, this touches
  `PersonalityBrief.swift`'s content; disjoint files, no shared interface.

### T7 — Retention policy confirmation with Kv
- **Backlog item:** #7 (`planning/engineering-plan.md`) / spec open question #4
- **Scope:** Check whether Kv has responded on retention/archiving policy (keep-forever + manual
  single-delete, as currently shipped, vs. a different policy such as soft-delete/archive or
  auto-expiry). If Kv wants a change: implement it, scoped to
  `ApinCore/Sources/Persistence/JournalRepository.swift` /
  `ApinCore/Sources/Persistence/SwiftDataJournalRepository.swift` and any UI surface in
  `Apin/Features/Journal/` needed to expose it — full scope depends entirely on what Kv asks for,
  so keep the implementation as small as the actual answer requires. If Kv confirms the
  as-shipped behavior (or hasn't responded): no code change, close/re-flag the open question in
  `memory/decisions.md`.
- **Dependencies:** none.
- **Acceptance criteria:**
  - If changed: new behavior implemented, unit-tested per `memory/coding-standards.md`'s testing
    strategy, `memory/decisions.md` updated with the confirmed new policy.
  - If unchanged: `memory/decisions.md`'s existing "Retention policy (keep-forever, manual
    single-delete only) implemented as assumed" entry is updated to confirmed-as-is (or
    re-flagged as still open if Kv genuinely hasn't answered), with no code change required for
    task completion.
- **Notes:** No hardware needed. Blocked on Kv's decision, not on other tasks. If T7 does result
  in a repository-layer code change, note that it would touch the same file T1 touches
  (`SwiftDataJournalRepository.swift`) — this is a *potential* file-level conflict if both are
  implemented in the same wave, not a hard dependency (T7's default/no-response outcome touches
  no code at all). If both T1 and T7 end up producing real code changes in the same file, prefer
  sequencing T7 after T1 lands to avoid a merge conflict, or coordinate via `/review`.

### T8 — T13 auto-submit-on-Spotlight-deep-link UX confirmation with Kv
- **Backlog item:** #8 (`planning/engineering-plan.md`) / spec open question #3 (partial)
- **Scope:** Check whether Kv has responded on whether Spotlight/Siri deep-link queries should
  keep auto-submitting immediately (`Apin/Features/Ask/AskViewModel.submit()` called
  automatically once `AskView` picks up a prefilled query from T13's App Intent deep link) or
  require a confirming tap first. If Kv wants a confirm-before-submit change: implement it,
  scoped to the T13 deep-link handling call site (wherever `AskView`/`AskViewModel.prefill(with:)`
  currently triggers auto-submit — search for the T13 deep-link wiring in `Apin/Features/Ask/` /
  `ApinCore/Sources/AppIntents/`) plus any minimal UI affordance needed for the confirming tap. If
  Kv confirms current auto-submit behavior (or hasn't responded): no code change, close/re-flag
  in `memory/decisions.md`.
- **Dependencies:** none.
- **Acceptance criteria:**
  - If changed: confirm-before-submit step implemented and unit-tested where feasible (view
    model state transition), `memory/decisions.md` updated with the confirmed new UX.
  - If unchanged: `memory/decisions.md`'s existing "T13's auto-submit-on-Spotlight-deep-link is
    unconfirmed product UX..." entry is updated to confirmed-as-is (or re-flagged still open),
    with no code change required for task completion.
- **Notes:** No hardware needed. Blocked on Kv's decision, not on other tasks. No file overlap
  with T1/T2/T3 (App Intent/deep-link wiring is a distinct call site from persistence,
  `project.yml`, or the capability gate).

### T9 — Re-spot-check minimum iOS/device requirement
- **Backlog item:** #9 (`planning/engineering-plan.md`) / spec open question #1
- **Scope:** Re-verify the iOS 26+ / A17 Pro-or-M-series-or-later minimum requirement against
  current Apple developer documentation (Apple Intelligence system requirements pages, Apple
  Developer docs for `FoundationModels`/`SystemLanguageModel`). This is a research/confirmation
  task, not a code change, unless a discrepancy is found — per the plan's explicit risk framing,
  if a discrepancy *is* found, treat it as a new, separate follow-up item rather than silently
  patching `project.yml`'s `deploymentTarget` or any hardware-check logic
  (`ApinCore/Sources/AI/` `OSVersionProviding`/`ModelHardwareAvailabilityProviding`
  implementations) within this task.
- **Dependencies:** none.
- **Acceptance criteria:**
  - Written confirmation (with source/date) that iOS 26+ and A17 Pro/M-series-or-later is still
    accurate, added to `memory/decisions.md` as a new entry alongside the existing "Deployment-
    target assumption... was verified, not just assumed" entry.
  - If a discrepancy is found: documented explicitly as a new backlog candidate for the next
    `/plan`, not silently patched here.
- **Notes:** No hardware needed — pure documentation research. Small, standalone, no
  dependencies either direction.

### T10 — Inline Siri/Spotlight answer latency spike (go/no-go)
- **Backlog item:** #10 (`planning/engineering-plan.md`) / spec Req 5, open question #3 remainder
- **Scope:** On the physical Apple Intelligence-capable device, measure real on-device inference
  latency for a representative Ask-flow question against Apple's App Intent execution time
  budget, to produce a documented go/no-go finding on whether an inline (non-deep-link)
  Siri/Spotlight answer is feasible. This is a measurement/spike task — no new App Intent or
  production code is expected to ship from this task regardless of outcome; the existing deep-link
  T13 behavior (shipped, cycle 1) remains the fallback either way. If the finding is "go," scope
  the actual inline-answer implementation as a new backlog candidate for the next `/plan` rather
  than building it inside this spike task.
- **Dependencies:** none.
- **Acceptance criteria:**
  - Documented latency measurement(s) from real hardware, compared explicitly against Apple's
    published/observed App Intent execution time budget.
  - A clear go/no-go conclusion recorded in completion notes and reflected in
    `memory/decisions.md` or `memory/technical-debt.md` as appropriate (e.g. "spike complete,
    no-go, T13 deep-link remains sole Spotlight path" or "spike complete, go, scoped as a new
    backlog item for cycle 3").
- **Notes:** **Requires a physical device (or an Apple Intelligence-capable Mac/Simulator actually
  running the model) for a meaningful latency number.** Same execution-environment caveat as
  T4/T5 — flag when queuing that this needs a device-connected session. Independent of T4/T5 (a
  different measurement target) though practically bundleable into the same physical-device
  session. A "no-go" outcome is an explicitly acceptable result per the plan's risk framing, not a
  task failure.

### T11 — Tagging: manual tag entry/edit + journal search tag filter
- **Backlog item:** #11 (`planning/engineering-plan.md`) / Req 10 (cycle-1 T20 carryover)
- **Scope:** Using the existing `tags: [String]` field already on `JournalEntry`
  (`ApinCore/Sources/Persistence/JournalEntry.swift`, currently unused/no UI writes to it) —
  add manual tag entry/edit UI on a journal entry (likely
  `Apin/Features/Journal/JournalEntryDetailView.swift` or a new small view in
  `Apin/Features/Journal/`) and a tag filter in journal search (extending
  `Apin/Features/Journal/JournalListView.swift`/`JournalDateRangeFilterView.swift`-adjacent
  filtering logic and `ApinCore`'s `JournalQuery` predicate building in
  `ApinCore/Sources/Persistence/JournalRepository.swift`/wherever `JournalQuery` is defined, to
  support an AND-combined tag predicate alongside existing keyword/date-range filtering).
- **Dependencies:** none.
- **Acceptance criteria:**
  - A journal entry's tags can be added/edited/removed via UI and persist through
    `JournalRepository.save(_:)`.
  - Journal search/filter UI supports filtering by tag, combined with existing keyword/date-range
    filters (AND-combined, consistent with `JournalQuery`'s existing combination behavior).
  - New logic in `ApinCore/Sources/Persistence` (query-building) has unit tests per
    `memory/coding-standards.md`.
  - `memory/database-schema.md`'s `tags` column note ("unused this cycle") is updated to reflect
    it's now consumed by UI.
  - SwiftLint: 0 new violations.
- **Notes:** No hardware needed. Nice-to-have priority — sequence after items 1–9 land with room
  to spare, per the plan. No dependency on T12 (weekly digest) — tagging and streak/digest
  computation don't share a code path (digest reads `createdAt`, not `tags`).

### T12 — Weekly digest / streak view
- **Backlog item:** #12 (`planning/engineering-plan.md`) / Req 11 (cycle-1 T21 carryover)
- **Scope:** New view (likely a new file under `Apin/Features/Journal/` or a new
  `Apin/Features/Digest/` folder, per `memory/coding-standards.md`'s folder-by-feature
  convention) computing questions-asked count and consecutive-day streak from
  `JournalEntry.createdAt` (read via `JournalRepository.fetchAll()`/`fetch(matching:)`). Streak
  and count computation logic should be a plain, testable function/type in
  `ApinCore/Sources/Persistence/` (or a new small `Sources/` subfolder if it doesn't fit
  persistence cleanly) rather than embedded directly in the SwiftUI view, per
  `memory/coding-standards.md`'s "SwiftUI views are not required to have tests unless they
  contain non-trivial logic... extracted out of the view" guidance.
- **Dependencies:** none.
- **Acceptance criteria:**
  - A weekly digest view exists and is reachable from the app's navigation, showing
    questions-asked count and current consecutive-day streak.
  - Streak/count computation logic is extracted into a testable unit with unit tests covering:
    no entries, single-day entries, multi-day consecutive streak, a broken streak (gap day).
  - SwiftLint: 0 new violations.
- **Notes:** No hardware needed. Nice-to-have priority — sequence after items 1–9 land with room
  to spare, per the plan. No dependency on T11 (tagging) — confirmed independent, digest logic
  only reads `createdAt`, tagging only touches `tags`; no shared file or interface beyond both
  reading through `JournalRepository`, which is already stable and not being changed by either
  task's core logic (T7's retention-policy task is the only one that might touch
  `JournalRepository` itself, and only if Kv requests a change).

## Blocked / notes

- **T4, T5, T10** are not blocked on hardware *availability* this cycle (Kv has confirmed an
  iPhone 17 + active Apple Developer account are available) but do require a **device-connected
  execution session** to actually run — this is an execution-environment concern for whoever
  picks up `/implement T4`/`/implement T5`/`/implement T10`, likely the human directly rather
  than a default headless `task-runner` session. Flag this at queue time; do not silently skip
  these tasks if the executing session lacks device access — instead report back that the task
  needs to be re-queued in a device-connected context, consistent with the plan's Definition of
  Done treating "verified blocked, documented" as an acceptable outcome for these three items
  specifically.
- **T6, T7, T8** depend entirely on whether Kv has actually responded to the underlying open
  questions by the time each task runs — check for Kv's answer (wherever it would surface: the
  orchestrating session, a note in `planning/spec.md`, or direct instruction) before defaulting
  to the "still open, re-flag in `memory/decisions.md`" outcome. A no-code-change completion is
  an acceptable, expected result for these three, not a failure.
- **T1 and T7** both touch `ApinCore/Sources/Persistence/SwiftDataJournalRepository.swift` *if*
  T7 results in an actual retention-policy code change (its default/no-response outcome touches
  no code). This is not modeled as a hard dependency in the table above because T7's outcome is
  unknown until it runs and most likely (pending Kv's response) produces no code diff at all —
  but if both are queued in the same wave and T7 turns out to require a code change, prefer
  landing T1 first and rebasing T7's diff, or route the conflict through `/review`.
- **`/review` (Cycle 2, 2026-08-11, first pass) sent T1, T3, and T11 back to `in-progress`** —
  code/tests for all three are correct and pass, but each missed an explicit, named
  acceptance-criteria item that updates a `memory/` file, and T3 additionally has no durable
  record of its required manual verification pass. See `review/review-report.md` for full detail.
  Specifically:
  - **T1**: `memory/technical-debt.md`'s "No DB-level uniqueness guarantee on `JournalEntry.id`
    post-CloudKit" entry is still `Status: open` — never updated to `resolved`, despite
    `fetch(by:)`/`delete(id:)`/`deduplicateEntries()` being correctly implemented and tested.
    Rework: update that entry's status/notes only, no code change needed.
  - **T3**: (a) same gap as T1, on the "Unsupported-device/OS path only structurally verified,
    never manually exercised" entry (still `Status: open`); (b) no record anywhere in the repo
    (`tasks/task-graph.md`, `memory/decisions.md`, `memory/technical-debt.md`, or elsewhere) of
    the required manual Simulator verification pass across all 5 `CapabilityGateResult` cases
    ever having actually been performed — the override mechanism itself is solid and verifiably
    absent from Release builds, but the task's other explicitly-owned half (render + observe +
    document all 5 cases) has no evidence of completion. Rework: actually perform that pass and
    record observations, then update the technical-debt.md entry.
  - **T11**: `memory/database-schema.md`'s `tags` column note still reads "unused this cycle — no
    UI writes to it... T20 (deferred, Nice-to-have) will consume it" — now factually wrong since
    T11 added tag entry/edit UI and a tag filter this cycle. Rework: update that column's Notes
    cell only, no code change needed.
- **T1 rework completed (2026-08-11), status returned to `in-review`.**
  `memory/technical-debt.md`'s "No DB-level uniqueness guarantee on `JournalEntry.id`
  post-CloudKit" entry is updated to `resolved (Cycle 2, T1)`, with a dated writeup describing
  the multi-match handling and `deduplicateEntries()` pass, matching the actual shipped code/
  tests. No code changed in this rework, as expected.
- **T3 rework completed (2026-08-11), status returned to `in-review`.** The manual Simulator
  verification pass across all 5 `CapabilityGateResult` cases was actually performed (Debug build,
  iPhone 17 simulator, iOS 26.3, via `SIMCTL_CHILD_APIN_DEBUG_CAPABILITY_OVERRIDE` +
  `xcrun simctl launch`/`screenshot`) and its evidence persisted at
  `review/verification-assets/t3-capability-cases/` (`README.md` with per-case observations +
  5 screenshots, one per case). All 5 cases rendered correct, crash-free UI matching
  `CapabilityStatusCopyProvider`. `memory/technical-debt.md`'s "Unsupported-device/OS path only
  structurally verified, never manually exercised" entry is updated to `resolved (Cycle 2, T3)`
  pointing at that evidence.
- **T11 rework completed (2026-08-11), status returned to `in-review`.**
  `memory/database-schema.md`'s `tags` column Notes cell is rewritten to describe the actual
  shipped behavior — consumed by `JournalEntryDetailView`'s tag entry/edit UI and
  `JournalQuery.search(keyword:dateRange:tag:)`'s tag filter, plus the in-memory
  (`JournalQuery.tagFilter`/`applyTagFilter(to:)`) post-fetch filtering caveat and the
  `#Predicate` limitation that necessitated it — no longer references the stale "unused this
  cycle" / "T20 will consume it" language.
- **`/review` (Cycle 2, 2026-08-11, follow-up pass) verified all three reworks close their
  specific gaps** (memory-file text confirmed accurate and current for T1/T11; T3's
  `review/verification-assets/t3-capability-cases/` evidence confirmed genuine — 5 distinct,
  correctly-sized, correctly-resolution PNG screenshots plus a README whose described copy was
  cross-checked word-for-word against `CapabilityStatusCopyProvider.copy(for:)` and matches
  exactly) and re-ran the full verification sweep (`swiftlint lint --strict`: 0 violations, 84
  files; `xcodebuild test` on the `Apin` scheme: 26 `ApinTests` + 7 `ApinWidgetTests`, 0 failures;
  `swift test` in `ApinCore`: 98/98 passing — 131 total, no regressions). **T1, T3, T11 marked
  `done`.** Cycle 2's full in-review scope (T1, T2, T3, T6, T7, T8, T9, T11, T12) is now `done`
  and ready for `/qa`. T4, T5, T10 remain `pending`, out of scope, device-blocked — not a defect.
