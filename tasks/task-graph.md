# Task Graph — Apin v1.3 (Cycle 4: language-scope decision + Spotlight invocation diagnosis)

Generated by `/tasks` (the `planner` subagent) from `planning/engineering-plan.md`.
Each task is implemented independently by `/implement <task-id>` (the `task-runner` subagent).
Tasks with no unfinished dependency can run in parallel.

**Cycle 4 context:** this overwrites Cycle 3's task graph (Cycle 3's T1-T5 are done — see
`memory/decisions.md`'s 2026-08-11 entries and `review/manual-verification/*.md`'s filled-in
Result sections). This cycle reuses the T1-T5 numbering fresh, mapped 1:1 to
`planning/engineering-plan.md`'s 5 backlog items: T1 is now the (ungated, sign-off already
recorded) English-only content swap, T2 is the build/metadata App Shortcut audit, T3 is the
Siri/Spotlight manual re-check script, T4 is its contingent fast-follow, and T5 is the routine
iOS-version-floor re-check.

- **T1, T2, T3, T5 are all immediately runnable** — none of them depend on another task in this
  cycle. In particular, **T3 does not depend on T2**: the plan is explicit that items 2 and 3
  investigate independent hypotheses for the same symptom (build/metadata-level vs.
  device/Settings/voice-level) and are complementary, not sequential — `/tasks` is not inventing
  an artificial dependency here. T3's script has a place to note T2's findings if they're
  available by the time T3 is written/executed, but T3 is fully executable without them.
- **T4 is contingent and starts `blocked`**, same pattern as Cycle 3's T5: it depends on T3
  *existing* (a real, expressible task-graph dependency) and, separately, on Kv actually executing
  T3's script on the physical device and reporting back — which is not something `/implement` can
  satisfy on its own and may not happen within this cycle's boundary. That is an acceptable,
  explicitly-tracked outcome per the plan's Definition of Done, not a task-graph failure.

## Tasks

| ID | Title | Depends on | Status | Owner agent |
|---|---|---|---|---|
| T1 | Shrink answer-language policy to English-only | — | done | task-runner |
| T2 | Build/metadata App Shortcut discoverability audit | — | done | task-runner |
| T3 | Manual verification script: Siri/Spotlight invocation re-check | — | done | task-runner |
| T4 | Fast-follow: record Kv's execution of T3's script | T3 | blocked | task-runner |
| T5 | Routine re-verification: iOS version floor | — | done | task-runner |

Status values: `pending → in-progress → in-review → done` (or `blocked` with a reason noted below).

T1, T2, T3, and T5 touch entirely disjoint surfaces — T1 edits one file
(`ApinCore/Sources/AI/PersonalityBrief.swift`), T2 runs a build and inspects its output plus two
existing Swift files without editing them (findings go into this file's task notes, not a new
file), T3 writes one new file under `review/manual-verification/`, and T5 is a live-doc-fetch
cross-check against `project.yml`/`ApinCore/Sources/AI/*` with no source edits expected absent a
discrepancy. There is no real reason to sequence any of them relative to each other — the only
constraint is `memory/coding-standards.md`'s iCloud-Drive parallelism cap (**max 2 `task-runner`
agents per wave**, this repo lives under iCloud Drive), so all four still need to run as two waves
of 2, not one wave of 4.

Suggested wave ordering:
1. **T1, T2** (both Must-priority, disjoint files/surfaces).
2. **T3, T5** (T3 Must-priority, T5 Should/routine — no reason to sequence T5 after T3 beyond the
   parallelism cap; grouping them keeps wave 1 at two Must items).
3. **T4** — not runnable as an implementation wave; stays `blocked` until Kv executes T3's script
   outside this session and reports back. May legitimately carry forward past this cycle's
   boundary (see `planning/engineering-plan.md` Risks and Definition of Done).

After each wave, run the standing iCloud-Drive stray-duplicate sweep from
`memory/coding-standards.md` (`find . -iname "* [0-9].*"` and `ls | grep xcodeproj`) before
trusting a build, per the established convention.

## Task details

### T1 — Shrink answer-language policy to English-only
- **Backlog item:** #1 (`planning/engineering-plan.md`) / spec open question #5 (product half,
  resolved) / `memory/decisions.md`'s 2026-08-11 "Answer language resolved: English-only" entry
- **Scope:**
  - `ApinCore/Sources/AI/PersonalityBrief.swift` only:
    - `PersonalityBrief.placeholder.languagePolicy`: replace the current value
      (`"Mirror the language of the question (English or Indonesian); default to English when the
      question's language is ambiguous."`) with an English-only instruction, e.g.
      `"Always respond in English, regardless of the question's language."` (exact wording is the
      task-runner's call within that intent — no Indonesian-mirroring language should remain).
    - Update the file's header doc comment (lines ~1-20): it currently frames open question #5 as
      unresolved and states `languagePolicy` "is carried forward unchanged" — both are now stale.
      Rewrite that paragraph to describe the language policy as confirmed English-only (per Kv's
      2026-08-11 sign-off), not carried-forward/unresolved.
  - Do **not** restructure `PersonalityBrief`'s type shape, rename `.placeholder`, or touch
    `PersonalitySystemInstructionBuilder.swift`'s building logic — content-only change, same
    discipline as Cycle 3's T1.
  - Do not touch `planning/spec.md` (this project's established convention, confirmed by reading
    prior cycles' handling of resolved open questions #2/#3/#4, is that `spec.md`'s Open Questions
    list is not hand-edited when a question resolves — `memory/decisions.md` is the record of
    resolution) or `memory/decisions.md` (the 2026-08-11 sign-off entry already exists there from
    the planning session).
  - Do not touch any other file.
- **Dependencies:** none
- **Acceptance criteria:**
  - `PersonalityBrief.placeholder.languagePolicy` no longer references mirroring or Indonesian;
    it unambiguously instructs English-only responses.
  - The file's header doc comment no longer describes `languagePolicy` as "carried forward
    unchanged" or open question #5 as unresolved.
  - `PersonalitySystemInstructionBuilderTests.swift` passes **unmodified** — verified during
    planning that its one language-related assertion
    (`testDefaultOutputIncludesLanguageHandlingInstruction`) reads
    `PersonalityBrief.placeholder.languagePolicy` dynamically rather than asserting a hardcoded
    string, so it should need no edits at all. If the task-runner finds this assumption wrong,
    that itself is worth flagging in completion notes, not silently working around.
  - `swift build` / `swift test` clean in `ApinCore`.
  - No other file changed (confirm with `git status`/`git diff --stat`).
- **Notes:** This is a content-only task; the swappable-brief structure already exists from
  Cycle 1's T5 and Cycle 3's T1. Item 1's sign-off is **not gated** for this task — Kv's
  confirmation is already recorded in `memory/decisions.md` (2026-08-11), so this task is
  immediately actionable, unlike how it was scoped in the engineering plan's earlier drafts.

### T2 — Build/metadata App Shortcut discoverability audit
- **Backlog item:** #2 (`planning/engineering-plan.md`) / `memory/decisions.md`'s 2026-08-11
  Siri/Spotlight entry, follow-up (c) / `memory/adrs/001-app-intents-split-across-apincore-and-app-target.md`
- **Scope:**
  - Re-run ADR 001's Cycle-1 T13 verification method against the **current** toolchain/SDK: build
    the `Apin` scheme (`xcodebuild -project Apin.xcodeproj -scheme Apin -destination
    'generic/platform=iOS' build`, or the simulator-destination equivalent if a generic-platform
    build isn't feasible in this session — either is acceptable as long as the App Intents
    metadata build phase runs) and locate/inspect the resulting
    `Metadata.appintents/extract.actionsdata` for `AskApinShortcutsProvider`'s `autoShortcuts`
    array: confirm it is still non-empty and both phrases ("Ask Apin a question" / "Ask a question
    in Apin") are still populated, matching ADR 001's documented finding.
  - Check the `appintentsnltrainingprocessor` build log output for the same "No AppShortcuts
    found - Skipping." failure signature ADR 001 documented, to confirm it is **not** recurring.
  - Read `Apin/AppIntents/AskApinShortcutsProvider.swift` and
    `ApinCore/Sources/AppIntents/AskApinIntent.swift` end to end and confirm nothing has changed
    since T13 shipped that would affect discoverability (compare against ADR 001's description and
    the file contents themselves — both currently match ADR 001, but verify rather than assume).
  - Confirm the Siri & Search / "Show in Search" capability surface: check `Apin/Apin.entitlements`
    (or `project.yml`'s `properties` for the `Apin` target) for anything relevant, and check
    `Info.plist`/`project.yml` for any `NSUserActivity`/Siri-related keys that might be
    missing/misconfigured.
  - Look for any other static, build/metadata-visible cause of a Spotlight-invocation gap (e.g., a
    missing `isDiscoverable` flag — already confirmed `true` in `AskApinIntent.swift` at line 45,
    re-verify it's still `true` — or a donation/indexing-adjacent code path that's silently absent).
  - Do **not** attempt any device-level fix or code change — this task is diagnostic/investigative
    only, per the plan's explicit framing ("item 2 itself is diagnostic, not a fix").
  - Record findings (pass/fail on `autoShortcuts`/`actionsdata`, build-log signature check result,
    entitlement/Info.plist check result, anything else found or ruled out) as a **Findings**
    subsection appended under this task's own section in this file (`tasks/task-graph.md`) — this
    task does not produce a new file under `review/manual-verification/` (that's T3's job) and does
    not itself write to `memory/decisions.md` (that's `/remember`'s job, reading this task's
    findings).
- **Dependencies:** none
- **Acceptance criteria:**
  - A build was actually run this cycle (not assumed identical to Cycle 1's T13 finding) and its
    `Metadata.appintents/extract.actionsdata` output was actually inspected — findings state
    exactly what was found (pass/fail on `autoShortcuts`, exact array contents or lack thereof),
    not a restatement of ADR 001's Cycle-1 result.
  - Findings explicitly state whether the `appintentsnltrainingprocessor` "No AppShortcuts found"
    failure signature recurred or not.
  - Findings explicitly state the result of the entitlement/Info.plist/`isDiscoverable` checks.
  - Findings are recorded in this file (a **Findings** subsection under T2, or equivalent) in
    enough detail for T3's script and `/remember` to use them without re-running the audit.
  - No app code changed as part of this task (it is diagnostic, not a fix) — if the audit surfaces
    something that clearly *is* a bug, flag it explicitly in findings as a new backlog candidate for
    the next `/plan` rather than fixing it inline.
- **Notes:** This is the same verification method ADR 001 used, re-run rather than assumed stable,
  per ADR 001's own "Consequences" section (SDK-version-dependent behavior). A clean pass here is a
  legitimate, useful result — it means the Spotlight-invocation gap Kv observed is more likely a
  runtime/device-state issue than a build-level defect, which narrows (not replaces) T3's
  investigation.

#### Findings (re-run 2026-08-11, Cycle 4)

**Toolchain:** Xcode 26.6 (Build 17F113), iPhoneOS26.5/iPhoneSimulator26.5 SDKs — a newer SDK
than ADR 001's Cycle-1 verification, per ADR 001's own note that this should be re-verified
rather than assumed stable.

**Build attempted:**
- `xcodebuild -project Apin.xcodeproj -scheme Apin -destination 'generic/platform=iOS' build` —
  **failed** (exit 65), but not for an App-Intents-related reason: `error: Signing for "Apin"
  requires a development team` / same for `ApinWidgetExtension`. This session has no development
  team configured for code signing, which the generic-platform (device) destination requires but
  the simulator destination does not. This is an environment-configuration gap, not a project
  defect — ADR 001's Cycle-1 T13 verification presumably ran in a session with a team configured.
- Fell back to the simulator-destination equivalent, as T2's scope explicitly allows:
  `xcodebuild -project Apin.xcodeproj -scheme Apin -destination 'platform=iOS
  Simulator,name=iPhone 17' build` — **succeeded** (exit 0). The App Intents metadata build
  phases (`appintentsmetadataprocessor`, `appintentsnltrainingprocessor`) ran as part of this
  build, satisfying the task's requirement that the metadata build phase actually runs.
- Before trusting the build, ran the standing iCloud-Drive stray-duplicate sweep per
  `memory/coding-standards.md`: found pre-existing, gitignored `Apin 2.xcodeproj` and
  `Apin 3.xcodeproj` directories at repo root (confirmed untracked/ignored via `git status
  --porcelain --ignored`, not part of any commit). These predate this task's build and are not
  referenced by the `xcodebuild -project Apin.xcodeproj ...` invocation used, so they did not
  affect this audit's result, but they are flagged below as a backlog candidate since the
  convention says to sweep for them, not silently ignore them once found.

**`autoShortcuts` / `extract.actionsdata` check — PASS.** Inspected
`<DerivedData>/Build/Products/Debug-iphonesimulator/Apin.app/Metadata.appintents/extract.actionsdata`
(the `Apin` app-target product, where `AskApinShortcutsProvider` lives per ADR 001). Its
`autoShortcutProviderMangledName` field is populated (`4Apin03AskA17ShortcutsProviderV`, the
mangled name of `Apin.AskApinShortcutsProvider`) and its `autoShortcuts` array is **non-empty**,
containing exactly one entry for `actionIdentifier: "AskApinIntent"` with `phraseTemplates`:
  - `"Ask ${applicationName} a question"`
  - `"Ask a question in ${applicationName}"`

  both present, `shortTitle` = "Ask Apin", `systemImageName` = "questionmark.bubble" — this
  matches `AskApinShortcutsProvider.swift`'s source and ADR 001's documented Cycle-1 finding
  exactly. The `actions` dictionary also has a populated `AskApinIntent` entry (not just an empty
  extraction), consistent with ADR 001's description of a correct build.

**Build-log failure-signature check — did NOT recur for the target that matters, but a
superficially similar line exists for an unrelated target (explained below).**
- The exact `appintentsnltrainingprocessor` invocation for the `Apin` app-target product
  (`--bundle-id com.apin.app`) logged: `Training 'Ask ${+applicationName} a question|Ask a
  question in ${+applicationName}' for en` / `Generated AppIntents SSU YAML files ...` — i.e. it
  **succeeded**, no "No AppShortcuts found - Skipping." for this target. This is the target ADR
  001's bug/fix was about, and it is clean.
  - Note the log's phrase syntax renders as `${+applicationName}` (bareword `+` flag), while
    `extract.actionsdata`'s `phraseTemplates` renders the same value as `${applicationName}`; both
    are the SSU/actionsdata tool's own internal representations of the same source phrase, not a
    discrepancy in our code — flagging only because it's easy to misread as two different phrases.
  - **A separate `appintentsnltrainingprocessor` invocation, for the `ApinWidgetExtension`
    target's product (`ApinWidget.appex`, `--bundle-id com.apin.app.ApinWidget`), did log "No
    AppShortcuts found - Skipping."** This is *not* a recurrence of the ADR 001 bug: `grep -rn
    "AppShortcutsProvider"` across `ApinWidget/` and `ApinCore/Sources/` (source, not `.build`
    index artifacts) returns zero matches — the widget extension has never had an
    `AppShortcutsProvider` conformance of its own (by design; `AskApinShortcutsProvider` lives
    only in the `Apin` app target per ADR 001), so this log line is the expected, correct output
    for a target with no App Shortcuts to register, not a discoverability defect. Confirmed only
    two `appintentsnltrainingprocessor` invocations ran in total (`Apin` and `ApinWidgetExtension`
    products) — `ApinCore` itself, being a library target with no bundled `Info.plist`, is not
    invoked with this tool at all, which is expected/unrelated to the ADR 001 mechanism (that bug
    was about `appintentsmetadataprocessor`'s cross-module extraction into `extract.actionsdata`,
    which *did* run against `ApinCore` and correctly picked up `AskApinIntent`).

**File read / `isDiscoverable` check — PASS, no drift since T13.**
- `git log --oneline` for both `Apin/AppIntents/AskApinShortcutsProvider.swift` and
  `ApinCore/Sources/AppIntents/AskApinIntent.swift` shows exactly one commit each
  (`5038611 feat: base apin app`) — neither file has been touched since T13 shipped, so there is
  no drift to reconcile against ADR 001's description.
- Read both files end to end: `AskApinShortcutsProvider.swift`'s `appShortcuts` still returns the
  single `AppShortcut(intent: AskApinIntent(), phrases: ["Ask \(.applicationName) a question",
  "Ask a question in \(.applicationName)"], shortTitle: "Ask Apin", systemImageName:
  "questionmark.bubble")` matching ADR 001. `AskApinIntent.swift` still declares
  `public static let isDiscoverable: Bool = true` at line 45 — re-verified directly, still `true`.
  `supportedModes = .foreground(.immediate)` and `perform()`'s deep-link-only body are also
  unchanged from ADR 001's description.

**Entitlement / Info.plist check — no gap found; nothing Siri/Search-specific is configured, and
nothing is missing that the App Intents (App Shortcuts) mechanism actually requires.**
- `Apin/Apin.entitlements` contains only `com.apple.developer.icloud-container-identifiers`,
  `com.apple.developer.icloud-services` (CloudKit), and `com.apple.security.application-groups` —
  no `com.apple.developer.siri` entitlement and no Siri/Search-specific entry.
- `Apin/Info.plist` (and its `project.yml` `properties`/`INFOPLIST_*` build settings) contain only
  `UIBackgroundModes: [remote-notification]` and the standard bundle-identity keys — no
  `NSUserActivityTypes`, no `NSSiri...` keys, no legacy `IntentsSupported` key anywhere in
  `project.yml`.
- This is **not** flagged as a gap: the modern App Intents / `AppShortcutsProvider` mechanism this
  app uses (confirmed working above, via `extract.actionsdata`/SSU YAML generation) is a
  build-metadata-driven registration path that does not require the legacy SiriKit
  `com.apple.developer.siri` entitlement or `NSUserActivityTypes` Info.plist declarations — those
  apply to the older Intents-extension/`NSUserActivity`-donation model, not to
  `AppShortcutsProvider`-based App Shortcuts. Empirically, this build produced a populated
  `autoShortcuts` array with zero entitlement/Info.plist configuration beyond what already
  exists, which corroborates that nothing is missing on this axis.

**Other static causes considered and ruled out:**
- No donation/indexing code path (e.g. `INInteraction` donation, `CSSearchableItem` indexing) is
  present anywhere in `ApinCore/Sources/AppIntents/` or `Apin/AppIntents/` — confirmed via file
  listing (only `AppIntentsScaffold.swift`, `AskApinIntent.swift`, `AskDeepLinkCoordinator.swift`
  in `ApinCore`, and only `AskApinShortcutsProvider.swift` in `Apin/AppIntents/`). Not flagged as
  a gap: App Shortcuts registered via `AppShortcutsProvider` do not require manual donation —
  registration happens automatically from the extracted metadata this task just confirmed is
  correct — so an absent donation path is expected, not a defect.

**Overall conclusion:** this cycle's re-run reproduces ADR 001's Cycle-1 result — build/metadata-
level App Shortcut discoverability is clean on the current toolchain (Xcode 26.6). No build-level
or static-config defect was found that would explain a Spotlight/Siri invocation gap on-device;
per this task's own framing, that narrows (does not replace) T3's device-level investigation —
the gap Kv observed is more likely a runtime/device-state issue.

**Backlog candidates surfaced (not fixed here, per this task's diagnostic-only scope):**
1. **Stray `Apin 2.xcodeproj` / `Apin 3.xcodeproj` directories at repo root**, gitignored and
   untracked, discovered during this task's pre-build stray-duplicate sweep. They did not affect
   this audit (the build targeted `Apin.xcodeproj` explicitly), but per
   `memory/coding-standards.md`'s "treat this as a standing constraint, not a one-time cleanup"
   guidance, they should be inspected and removed (if confirmed to be stale iCloud conflicted
   copies) in a follow-up task — not fixed inline here since T2 is diagnostic-only and this isn't
   app code but is still outside this task's stated scope.
2. **No genuine bug found** in `AskApinShortcutsProvider.swift`, `AskApinIntent.swift`,
   entitlements, or `Info.plist`/`project.yml` — nothing else to flag as a backlog candidate from
   this audit's actual scope.

### T3 — Manual verification script: Siri/Spotlight invocation re-check
- **Backlog item:** #3 (`planning/engineering-plan.md`) / `memory/decisions.md`'s 2026-08-11
  Siri/Spotlight entry, follow-ups (a)/(b) / spec Req 5 (Must), open question #3 remainder
- **Scope:**
  - New file `review/manual-verification/siri-spotlight-invocation-recheck.md` (do not overwrite
    or edit the existing, already-filled-in
    `review/manual-verification/siri-spotlight-latency-spike.md` — that script's Result section is
    Kv's historical record from Cycle 3 and stays as-is): a precise, self-contained checklist for
    Kv to execute on the physical iPhone 17, same self-contained-script convention as Cycle 3's
    three scripts (exact steps, exact pass/fail criteria, blank Result section, no need to re-read
    the engineering plan to execute it), covering:
    - **(a)** Retry invocation via **Siri voice** specifically (not just typed Spotlight search,
      which is what Cycle 3's check used and which produced no result) — isolate whether the gap
      is Spotlight-specific or affects both invocation surfaces.
    - **(b)** Re-check **Settings > Siri & Search > Apin** — "Show in Search" and "Learn from this
      App" (or the current iOS 26 equivalents) toggles, same ones the prior script's Prerequisites
      section named, confirming their actual current state (not assuming they're already correct).
    - **(c)** A designated section to note T2's build/metadata findings, if available at the time
      Kv runs this script, so Kv can cross-reference ("here's what the build-level check found;
      does that match what you observe") — populate this section with T2's actual findings if T2
      is `done` by the time this task runs; if T2 hasn't completed yet, leave this section with a
      clear placeholder pointing to `tasks/task-graph.md` T2's Findings subsection, since **this
      task does not wait on T2** (see the cycle-context note above).
  - No app code changes.
- **Dependencies:** none — explicitly **not** T2. The plan is explicit that items 2 and 3
  investigate independent hypotheses in parallel; this task must not be blocked on T2's status.
- **Acceptance criteria:**
  - Script is precise enough that Kv can execute it without re-reading
    `planning/engineering-plan.md` or asking a clarifying question first.
  - Script includes distinct, explicit steps for Siri-voice invocation (not just typed Spotlight)
    and for re-checking the two named Settings toggles, each with its own pass/fail criteria.
  - Script has a section referencing/cross-referencing T2's findings (populated if T2 is done,
    clearly marked pending otherwise) without making script execution depend on T2.
  - "Result" section exists and is genuinely blank (not pre-filled or guessed).
- **Notes:** Same convention/discipline as Cycle 3's T2-T4: this task is **done** when the script
  exists, is precise, and is hand-off-ready — not when the underlying invocation gap is actually
  diagnosed. Closing it out is T4, contingent on Kv's report.

### T4 — Fast-follow: record Kv's execution of T3's script
- **Backlog item:** #4 (`planning/engineering-plan.md`)
- **Scope:**
  - Once Kv has executed T3's script (`review/manual-verification/siri-spotlight-invocation-recheck.md`)
    and reported results (via chat or by filling in the script's Result section directly), update:
    - `memory/decisions.md`: a new dated entry recording the actual outcome — whether Siri-voice
      invocation succeeded/failed, the actual state of the two Settings toggles, and how the result
      compares to T2's build/metadata findings.
    - `memory/technical-debt.md` and/or spec open question #3, if warranted by the outcome
      (resolved with evidence, or re-confirmed still-open with a dated note), same pattern as
      Cycle 3's item 5/T5.
  - No app code changes unless the result surfaces a genuine, clearly-scoped defect requiring one —
    if so, that's a new backlog item for a future cycle, not silently absorbed into T4.
- **Dependencies:** T3 (script must exist first) — plus, not expressible as a task-graph
  dependency, Kv's actual execution and report, which happens outside this session.
- **Acceptance criteria:**
  - `memory/decisions.md`'s new entry reflects Kv's actual reported outcome, not an assumption or
    a guess.
  - If Kv has not reported by the time `/qa` runs this cycle, this task stays `blocked` and
    carries forward — an acceptable, explicitly-tracked outcome per the plan's Definition of Done,
    not a failure to fix before `/qa`.
- **Notes:** Do not self-certify this task by reasoning about what Kv's device would "probably"
  show — a static-analysis argument does not satisfy this task, per the plan's explicit framing
  that item 3 (and therefore item 4) is human-in-the-loop, not task-runner-executable. This is the
  fourth consecutive cycle touching this class of device-verification blocker (per the plan's Open
  Questions #3) — if T4 is still `blocked` when this cycle closes, that itself is worth surfacing
  to Kv again rather than quietly carrying forward unremarked.

### T5 — Routine re-verification: iOS version floor
- **Backlog item:** #5 (`planning/engineering-plan.md`) / spec open question #1
- **Scope:**
  - Re-verify the iOS 26+ / A17 Pro-or-M-series-or-later minimum against **current** Apple
    developer documentation (same method as Cycle 2's T9 and Cycle 3's re-check — live doc fetch,
    not a memory of the prior result): `FoundationModels`/`SystemLanguageModel` availability floor,
    and Apple's Apple Intelligence device-support page for the minimum-eligible chip/device list.
  - Cross-check `project.yml`'s `deploymentTarget` (currently `"26.0"` on all relevant targets) and
    every `@available(...)` annotation in `ApinCore/Sources/AI/` for consistency with whatever this
    cycle's fetch finds.
  - If no discrepancy: record a decisions-log-style confirmation, same format as the prior two
    cycles' entries. If a discrepancy is found: flag it explicitly (do not silently fix
    `project.yml` or `@available` annotations as part of this task — surface it as a new backlog
    candidate for the next `/plan`, consistent with T2/T5's diagnostic-not-fix framing elsewhere
    this cycle).
- **Dependencies:** none
- **Acceptance criteria:**
  - Findings cite the actual sources fetched this cycle (URLs), not a restatement of Cycle 2/3's
    findings.
  - `project.yml` and `ApinCore/Sources/AI/` `@available` annotations are explicitly cross-checked
    and the result (consistent / inconsistent) is stated.
  - No source file is edited unless a genuine discrepancy is found and even then only after
    surfacing it rather than silently patching — same discipline as T2.
  - Findings recorded in this file (a **Findings** subsection under T5) for `/remember` to fold
    into `memory/decisions.md`, following the exact entry format Cycle 2's T9 and Cycle 3's
    equivalent used (see `memory/decisions.md`'s 2026-08-11 "iOS 26+ ... re-verified" entry for the
    expected shape).
- **Notes:** Routine, low-risk, Should-priority. `memory/decisions.md`'s Open Questions note (via
  the engineering plan) flags that Kv may want to consider retiring this from active per-cycle
  tracking now that it's been stable across three independent checks — this task doesn't decide
  that unilaterally, it just performs the check and notes the standing suggestion again if still
  stable.

#### Findings (re-run 2026-08-11, Cycle 4)

Live-fetched two current Apple sources today (2026-08-11, same day as Cycle 4's other tasks, a
separate fetch from the 2026-08-11 T9 entry in `memory/decisions.md` — re-run per this task's own
"live doc fetch, not a memory of the prior result" scope, not a restatement):

- Apple Developer documentation for `FoundationModels`/`SystemLanguageModel`, fetched via its
  markdown mirror at `https://docs.developer.apple.com/tutorials/data/documentation/
  foundationmodels/systemlanguagemodel.md` (the canonical
  `https://developer.apple.com/documentation/foundationmodels/systemlanguagemodel` URL 301-redirects
  in this environment's `curl`, so the markdown mirror — the same one the prior cycle's fetch
  used — was fetched directly instead, following redirects with `-L`). Its embedded metadata
  comment lists the availability floor as `iOS: 26.0.0 -`, `iPadOS: 26.0.0 -`,
  `macCatalyst: 26.0.0 -`, `macOS: 26.0.0 -`, `visionOS: 26.0.0 -` — unchanged from the prior
  cycle's fetch. The page body still describes exactly 3 on-device model versions, aligned to
  "26.0 - 26.3", "26.4", and "27.0" — no new model version and no floor change since the last
  check.
- Apple's official Apple Intelligence device-support page (`https://www.apple.com/apple-intelligence/`,
  fetched today with a desktop-browser `User-Agent` header) lists, after stripping HTML: iPhone 17
  Pro Max/17 Pro/Air (A19 Pro), iPhone 17/17e (A19), iPhone 16 Pro Max/16 Pro (A18 Pro), iPhone 16
  Plus/16/16e (A18), and — as the minimum-eligible iPhones — **iPhone 15 Pro Max / iPhone 15 Pro
  (A17 Pro)**; iPad Pro/iPad Air with "M1 and later"; iPad mini with "A17 Pro"; Apple Vision Pro
  with "M2 and later"; and Mac models (MacBook Neo A18 Pro, MacBook Air/Pro, iMac, Mac mini, Mac
  Studio, Mac Pro) all "M1 and later" or newer. This still matches "A17 Pro-or-M-series-or-later"
  exactly — no discrepancy, and no new lower-end device added to the list since the last check.

Cross-checked against the current codebase for consistency (not changed, per this task's scope):
- `project.yml` sets `deploymentTarget: "26.0"` on all 4 relevant targets (`options.deploymentTarget.iOS`
  plus each of `Apin`, `ApinTests`, `ApinWidgetTests`, `ApinWidgetExtension`) — read directly,
  confirmed unchanged.
- Every `@available(...)` annotation in `ApinCore/Sources/AI/` gates at `iOS 26.0`: confirmed via a
  fresh `grep -rn "@available" ApinCore/Sources/AI/`, which returns exactly 6 hits —
  `AssistantSessionService.swift`, `LanguageModelSessionProviding.swift`,
  `FoundationModelsSessionAdapter.swift`, and `AsyncTimeout.swift` at `iOS 26.0, macOS 26.0,
  visionOS 26.0, *`; `ModelHardwareAvailabilityProviding.swift` and `CapabilityGate.swift` at
  `iOS 26.0, macOS 26.0, *`. `OSVersionProviding.swift` has no `@available` attribute (it's the
  runtime check itself, not an availability-gated declaration) but its `DefaultOSVersionProvider
  .isRunningSupportedOSVersion` body uses `if #available(iOS 26.0, macOS 26.0, *)` — same floor,
  read directly and confirmed unchanged.
- Re-confirmed `ModelHardwareAvailabilityProviding.swift`'s
  `DefaultModelHardwareAvailabilityProvider` still does not hardcode a chip/device allowlist —
  it still defers entirely to `SystemLanguageModel.default.availability` at runtime, so a future
  Apple device-list change (adding a new minimum chip, say) would not require a code change here,
  only a `project.yml` bump if Apple ever raises the OS-version floor itself.

**Result: consistent — no discrepancy found this cycle.** Both live-fetched Apple sources match
the codebase's existing `iOS 26.0` / A17 Pro-or-M-series-or-later assumption exactly, with no
drift from either the 2026-08-10 original verification or the 2026-08-11 T9 re-check earlier this
same cycle. No new backlog candidate needed on this axis. Per this task's Notes: this is the
fourth independent check across four cycles (2026-08-10 original, Cycle 2's T9, Cycle 3's
re-check, this cycle's T9 and now T5) to find the same stable result — re-surfacing, not
deciding, the standing suggestion that Kv may want to consider retiring this from active
per-cycle tracking, now that it has never once moved.

## Blocked / notes

- **T4**: `blocked` from the start of this cycle — depends on T3 existing (real, task-graph
  dependency) and, separately, on Kv executing T3's script on the physical device and reporting
  back, which cannot happen inside this implementation session. Same pattern as Cycle 3's T5,
  which did unblock and complete within its cycle after Kv reported back same-day — T4 may or may
  not follow that same timeline; either outcome is acceptable per the plan's Definition of Done.
