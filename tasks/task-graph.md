# Task Graph — Apin v1.4 (Cycle 5: design-handoff foundation, Stage A only)

Generated by `/tasks` (the `planner` subagent) from `planning/engineering-plan.md` v1.4.
Each task is implemented independently by `/implement <task-id>` (the `task-runner` subagent).
Tasks with no unfinished dependency can run in parallel.

**Cycle 5 context:** this overwrites Cycle 4's task graph (Cycle 4's T1, T2, T3, T5 are `done`;
T4 stayed `blocked` and is carried forward unchanged as this cycle's backlog item 12 — see
`memory/decisions.md`'s 2026-08-11 entries). Per Kv's explicit 2026-08-12 scope decision (recorded
in `memory/decisions.md`), this graph covers **Stage A only**: `planning/engineering-plan.md`'s
backlog items 1-7 (design-token layer, structured `@Generable` output, excerpt derivation,
auto-tags seeding, root navigation composition) plus the carried-forward item 12 (Siri/Spotlight
device-recheck fast-follow). **Stage B (backlog items 8-11 — the pixel-accurate Ask/Journal screen
recreation and the search/filter re-skin) is out of scope for this graph** — deferred to a
follow-up cycle once Stage A lands and is reviewed, per the plan's own "How to read this plan"
section and Kv's decision. No T-ids exist for Stage B items in this file; see
`planning/engineering-plan.md`'s Stage B table for that scoped-but-deferred work when the
follow-up cycle's `/plan` picks it up.

This cycle reuses fresh `T1`-`T6` numbering (same convention Cycle 4 used: restart numbering each
cycle rather than continue a running count), mapped as: **T1** = items 1-3 (design tokens, font,
icons — bundled, see rationale below), **T2** = item 4 (structured `@Generable` output), **T3** =
item 5 (excerpt derivation), **T4** = item 6 (auto-tags seeding, depends on T2), **T5** = item 7
(root navigation composition), **T6** = item 12 (carried-forward Siri/Spotlight fast-follow, stays
`blocked`, same as Cycle 4's T4).

- **T1, T2, T3, and T5 are all immediately runnable** — none of them depend on another task in
  this graph. They touch entirely disjoint files (see each task's Scope), so there is no
  structural reason to sequence them relative to each other beyond the parallelism cap below.
- **T4 depends on T2** — this is a real dependency, not an invented one: item 6's "seed
  `JournalEntry.tags` from the structured call's output" needs item 4's structured call to exist
  and actually produce a `tags` array first. T4 cannot start meaningfully until T2 is `done`.
- **T6 stays `blocked` from the start**, same pattern as Cycle 4's T4: it has no task-graph
  dependency in this cycle (the script it waits on,
  `review/manual-verification/siri-spotlight-invocation-recheck.md`, already exists — produced by
  Cycle 4's T3), but it is gated on Kv actually executing that script on the physical device and
  reporting back, which `/implement` cannot do on its own. This is the fifth consecutive cycle
  touching this class of device-verification blocker (Cycles 3 and 4 both carried it forward) —
  worth surfacing to Kv again, not quietly re-carrying it.

**Why items 1-3 are bundled into one task (T1) rather than three:** the plan itself ties item 2
(font) explicitly to item 1's token layer ("Apply as reusable `Font`/text-style helpers in the
token layer (item 1)"), and item 3 (icons) is a small, independently-trivial mapping decision with
no meaningful acceptance-testable surface of its own separate from "the token layer exists and
uses SF Symbols, not Phosphor." Splitting these into three sequential tasks (2 and 3 blocked on 1)
would add coordination overhead without adding independent reviewability — a fake three-task
dependency chain for what is, in substance, one new `Apin/DesignSystem/` surface. Bundling keeps
the real per-item acceptance criteria from the plan (see T1's Acceptance criteria below, which
still enumerates all three items' bars separately) while avoiding an artificial 1→2, 1→3 wait.

## Tasks

| ID | Title | Depends on | Status | Owner agent |
|---|---|---|---|---|
| T1 | Design-system foundation: tokens, font, icon decisions | — | done | task-runner |
| T2 | Structured `@Generable` model output (`AskResponse`) | — | done | task-runner |
| T3 | Excerpt derivation helper (first-sentence, non-stored) | — | done | task-runner |
| T4 | Auto-tags seeding on save | T2 | done | task-runner |
| T5 | Root navigation composition (Ask ↔ Journal) | — | done | task-runner |
| T6 | Fast-follow: record Kv's execution of the Siri/Spotlight invocation re-check script | — (contingent on Kv, see notes) | blocked | task-runner |

Status values: `pending → in-progress → in-review → done` (or `blocked` with a reason noted below).

T1, T2, T3, and T5 touch entirely disjoint surfaces — T1 adds only new files under a new
`Apin/DesignSystem/` folder, T2 touches only `ApinCore/Sources/AI/` (plus its test files), T3 adds
only a new file (and test) under `ApinCore/Sources/Persistence/`, and T5 touches only
`Apin/ContentView.swift` and `Apin/Features/Ask/AskView.swift` (removing the digest toolbar
button per Kv's 2026-08-12 decision) plus possibly `Apin/Features/Journal/JournalListView.swift`
(placeholder digest entry point / navigation-stack reconciliation). T4 depends on T2 but, once T2
is `done`, touches a different file set from it (`Apin/Features/Ask/AskAndSaveServicing.swift` /
`AskAndSaveService.swift`, not `ApinCore/Sources/AI/`).

Per `memory/coding-standards.md`'s iCloud-Drive parallelism cap (**max 2 `task-runner` agents per
wave**, this repo lives under iCloud Drive), suggested wave ordering:

1. **T1, T2** — both foundational, both Must-priority, fully disjoint files.
2. **T3, T5** — both Must-priority, fully disjoint files, no dependency on wave 1's output (can
   run before, after, or during wave 1 in principle; grouped into wave 2 only because of the
   2-per-wave cap, not because of a real ordering requirement).
3. **T4** — depends on T2 (wave 1), so cannot start until wave 1 completes; runs alone (or could
   pair with a Stage-B task in a future cycle, but there is none in this graph).
4. **T6** — not an implementation wave; stays `blocked` pending Kv's execution/report outside this
   session (see T6's own section). May legitimately carry forward past this cycle's boundary.

After each wave, run the standing iCloud-Drive stray-duplicate sweep from
`memory/coding-standards.md` (`find . -iname "* [0-9].*"` and `ls | grep xcodeproj`) before
trusting a build.

## Task details

### T1 — Design-system foundation: tokens, font, icon decisions
- **Backlog items:** #1, #2, #3 (`planning/engineering-plan.md`) — bundled, see rationale above.
- **Scope:**
  - New folder `Apin/DesignSystem/` (app target only — mirrors how `Apin/Common/` already exists
    for `StatusMessageView.swift` as a non-feature, cross-cutting UI folder; per
    `memory/coding-standards.md`'s folder-by-feature convention extended for cross-cutting UI, per
    the plan's own Architecture Decisions section).
  - Translate `design_handoff_apin/apin-green.css`'s `:root` tokens (OKLCH colors, `--radius-*`,
    `--space-*`, type scale) into Swift `Color`/spacing/radius constants. Exact file
    breakdown inside `Apin/DesignSystem/` (e.g. one `DesignTokens.swift` vs. split
    `Color+Apin.swift`/`Spacing+Apin.swift`/`Radius+Apin.swift`) is the task-runner's call, subject
    to `memory/coding-standards.md`'s one-primary-type-per-file rule where it applies to a real
    type (a flat namespace `enum`/`struct` of constants is not a "type per constant," so grouping
    related constants in one file is fine).
  - **Colors:** pre-convert every `oklch(...)` value to sRGB/Display P3 component values at
    token-definition time (no runtime OKLCH computation — no SwiftUI/UIKit OKLCH initializer is
    known to exist as of iOS 26, per the plan's Architecture Decisions; verify this against current
    Apple documentation as a first implementation step and note the finding in completion notes,
    same rigor the plan asks for). Keep the source `oklch(...)` string as an adjacent comment on
    every converted constant so a future re-tune of `apin-green.css` can be re-derived, not
    reverse-engineered.
  - **Font (item 2):** use SF Pro (system font), not a bundled Inter — no font asset exists in the
    repo today (confirmed via `Glob`, unchanged this cycle). Expose reusable `Font`/text-style
    helpers (e.g. a `Font` extension or a small `AppinTypography`-style namespace) inside the token
    layer at the handoff's specified weights/sizes, for future call sites (Stage B) to use instead
    of ad hoc per-view `.font(...)` calls. This task does not need to apply these fonts to any
    existing view — no `Features/*` view is touched by this task.
  - **Icons (item 3):** map the handoff's three named icons (`book-open-text`, `bookmark-simple`,
    `arrow-up`) to SF Symbol equivalents (not the Phosphor SwiftUI package — this project has zero
    external SPM dependencies today, confirmed via `project.yml`'s `packages:` section, and three
    icons don't justify the first one). Expose these as named constants (e.g. `Image(systemName:
    AppinIcon.journal)`-style) in the token layer. Confirm visual fit against the handoff's
    regular-weight Phosphor reference during implementation (a documented judgment call, not a
    formal test).
  - Does **not** touch `project.yml` (no new SPM dependency), `ApinCore/` (this is inherently a
    SwiftUI/UIKit-adjacent surface — `ApinCore` must never import SwiftUI/UIKit, per
    `memory/coding-standards.md` and `JournalEntry.swift`'s own header), or any existing
    `Features/*` view (nothing consumes these tokens yet this cycle — Stage B is the consumer,
    deferred).
- **Dependencies:** none.
- **Acceptance criteria:**
  - `grep -rn "oklch(" Apin/DesignSystem/` returns matches **only** inside comments (i.e. no
    `oklch(...)` string is used as a live/parsed value anywhere in Swift source) — the plan's
    Definition of Done bullet for items 1-3 stated generally, checked here concretely.
  - `grep -rn "^import SwiftUI\|^import UIKit" ApinCore/Sources/` still returns zero matches
    (unchanged invariant — confirms T1 didn't leak UI types into `ApinCore`).
  - `project.yml`'s `packages:` section is unchanged (still only the local `ApinCore` path) — no
    Phosphor or other new SPM dependency added.
  - Color constants exist for (at minimum) every color the handoff's "Design tokens" table and
    `apin-green.css`'s `:root` block name, each with an adjacent `oklch(...)` source comment.
  - Font helpers exist covering the handoff's specified type scale/weights, backed by SF Pro
    (system font), not a bundled font file.
  - SF Symbol constants exist for `book-open-text`, `bookmark-simple`, and `arrow-up`'s mapped
    equivalents.
  - `swift build` (ApinCore) unaffected; `xcodebuild ... build` for the `Apin` scheme succeeds with
    the new files added (no consumer yet, so this is purely a "does it compile" check).
  - No `Features/*` file changed (confirm via `git diff --stat`) — this task is additive-only.
- **Notes:** Stage B (deferred) is what will actually apply these tokens/fonts/icons to
  `AskView`/`JournalListView`/etc. — this task's job is to make that later work a drop-in
  consumption of an existing, reviewable source of truth, not to touch any current screen. If the
  OKLCH→sRGB/P3 conversion tooling/method needs a library or script, prefer a one-off conversion
  documented in a code comment (with the derivation method noted) over adding a runtime dependency
  — consistent with the "zero new SPM dependency" constraint above.

**Completion notes (2026-08-12):** Implemented as scoped: `Apin/DesignSystem/ApinColor.swift`,
`ApinFont.swift`, `ApinIcon.swift`, `ApinRadius.swift`, `ApinSpacing.swift` — 5 files, all
`case`-less namespace `enum`s. OKLCH runtime-initializer check confirmed directly (not assumed):
neither `SwiftUICore.Color.RGBColorSpace` (`.sRGB`/`.sRGBLinear`/`.displayP3` only) nor
`UIKit.swiftinterface` has an OKLCH case/initializer, checked by reading both `.swiftinterface`
files. Every color is pre-converted OKLab→linear sRGB→gamma sRGB via Björn Ottosson's public
OKLab matrices, constructed with `Color(.sRGB, red:green:blue:opacity:)`, with the source
`oklch(...)` string as an adjacent comment. The task-runner agent was interrupted mid-build-check
by the user; the orchestrator completed verification directly afterward: every `--color-*`,
`--radius-*`, and `--space-*` token in `design_handoff_apin/apin-green.css`'s `:root` block is
covered (diffed the full token list against the file contents — complete, nothing missed);
`grep -n "oklch("` in `Apin/DesignSystem/` matches only inside `///`/`//` comments;
`ApinCore/Sources/` still has zero `^import SwiftUI`/`^import UIKit` matches; `project.yml`'s
`packages:` section unchanged (only `ApinCore`); `git status --porcelain` confirms no
`Features/*` file touched by T1 (the one `Features/*` diff present,
`Apin/Features/Ask/AssistantSessionProviding.swift`, is T2's change). Icons:
`book-open-text`→`book.pages`, `bookmark-simple`→`bookmark`, `arrow-up`→`arrow.up` (SF Symbols,
each confirmed present in the installed SF Symbols catalog, well below the iOS 26 floor). Font:
SF Pro via `Font.system(size:weight:design:)`, no bundled font file.

Full `xcodebuild -project Apin.xcodeproj -scheme Apin -destination generic/platform=iOS build`
initially failed on **code signing** ("requires a development team") — a pre-existing
project-config/environment gap unrelated to any task's source changes (no dev team configured in
this sandbox), not a T1 defect. Re-ran with `CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO
CODE_SIGN_IDENTITY=""` (compile/link-only, standard for a headless "does it build" check) —
`** BUILD SUCCEEDED **`, all targets (`Apin`, `ApinWidgetExtension`, `ApinCore` via SPM),
including the new `Apin/DesignSystem/*.swift` files compiling cleanly alongside T2's concurrent
`ApinCore` changes. Flagging the missing dev-team config as a standing environment note for
`/qa` and any future task needing an actual signed device/simulator run — out of scope to fix
here.

Stray `Apin 2.xcodeproj`/`Apin 3.xcodeproj` (flagged by T2) confirmed harmless: both contain only
gitignored per-user `xcuserdata` (no `project.pbxproj`, not real duplicate projects, invisible
to `git status`) — no action needed, not a build hazard.

**`/review` finding (2026-08-12), status set back to `in-progress`:** `swiftlint lint` (run
independently, not just the build check T1's own notes report) surfaces **7 `identifier_name`
warnings**, all confined to this task's new files: `Apin/DesignSystem/ApinFont.swift`'s `h1`, `h2`,
`h3`, `h4`, `h5`, `h6` (lines 49/52/55/58/61/65) and `Apin/DesignSystem/ApinColor.swift`'s `bg`
(line 49) — each below SwiftLint's default 3-character minimum. `memory/coding-standards.md`
documents this project's standing bar as "0 SwiftLint violations at any point" (held since Sprint
1); this is the first cycle to introduce new lint output, and it wasn't caught or disclosed in this
task's own completion notes (which verify the build but never ran/reported `swiftlint` for T1's own
files, unlike T2/T4/T5's completion notes, which each state "swiftlint: 0 violations"). Every other
file this cycle touched is confirmed lint-clean (`/review` re-ran `swiftlint lint --quiet` scoped to
each of T2/T3/T4/T5's changed files individually — zero output).

**Fix needed before this task is `done`:** rename `h1`-`h6` to something like
`heading1`-`heading6` (or `ApinFont.h1` → `ApinFont.heading1`, etc.) and `bg` to `background` (or
add a scoped `// swiftlint:disable:next identifier_name` with a one-line justification per
constant, matching this file's existing terse-comment convention, if the shorter names are
preferred for call-site brevity). Not a functional defect — `Apin/DesignSystem/` has zero
consumers this cycle (Stage B is deferred), so this doesn't block `/qa`, which has nothing to
exercise here; it only needs to land before this task is marked `done`. See
`review/review-report.md`'s Naming check section for full detail.

**Fix applied (2026-08-12), status set to `done`:** kept the short names (traceability to
`apin-green.css`'s token names outweighed call-site-brevity concerns for a design-token layer) and
added a scoped `// swiftlint:disable:next identifier_name` above each of the 7 flagged
declarations, with a one-line justification comment. Two ordering iterations were needed: a `///`
doc comment separated from its declaration by the disable-command line trips
`orphaned_doc_comment`, and `:next` only silences the line immediately below it — landed on plain
`//` description comment(s) directly above `// swiftlint:disable:next identifier_name`, immediately
above the declaration, no `///` on these specific lines. Re-verified: `swiftlint lint
Apin/DesignSystem` → 0 violations; `swiftlint lint` (whole repo) → **0 violations across 92
files**, restoring the standing "0 violations at any point" bar; `xcodebuild -project
Apin.xcodeproj -scheme Apin -destination "generic/platform=iOS" build CODE_SIGNING_ALLOWED=NO
CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY=""` → `** BUILD SUCCEEDED **`, re-run in full.

### T2 — Structured `@Generable` model output (`AskResponse`)
- **Backlog item:** #4 (`planning/engineering-plan.md`) / Architecture Decisions' "Structured model
  output via Foundation Models guided generation" entry / Risks' "Structured-generation streaming
  is an unverified assumption" entry.
- **Scope:**
  - `ApinCore/Sources/AI/AssistantSessionService.swift`: add a new structured-generation entry
    point using Foundation Models' guided generation (`@Generable`/`@Guide`), producing a value
    shaped `{answer: String, followUpQuestion: String, chips: [String], tags: [String]}` (proposed
    name `AskResponse` per the plan; exact macro/parameter syntax must be confirmed against current
    Apple documentation during implementation — the plan explicitly flags this as **not**
    live-verified this planning session, unlike the routine iOS-floor checks in prior cycles).
  - `ApinCore/Sources/AI/LanguageModelSessionProviding.swift` and
    `ApinCore/Sources/AI/FoundationModelsSessionAdapter.swift`: extend the session seam with
    whatever new method(s) the structured call needs (Foundation Models' real `LanguageModelSession`
    exposes a `generating:`-style guided-generation variant distinct from the plain-`String`
    `respond(to:)`/`streamResponse(to:)` this seam currently wraps) — this is a structurally
    necessary consequence of adding the new capability through this seam, not scope creep; the plan
    names `AssistantSessionService.swift`/`AssistantSessionProviding.swift` explicitly but the
    lower-level session-adapter files are the mechanism those two are built on.
  - `Apin/Features/Ask/AssistantSessionProviding.swift`: mirror the new structured method(s) on
    this protocol, per this file's own stated design ("mirrors `AssistantSessionService`'s ...
    API exactly").
  - **First implementation step, not assumed:** verify against current Apple documentation whether
    Foundation Models' streaming path supports incremental partial-`@Generable` snapshots (needed
    for a future streaming UI over a *structured* response). Document the finding either way in
    this task's completion notes — this is load-bearing context for whoever implements the
    deferred Stage B item 9 later, which depends on this answer. If unsupported, the documented
    fallback (per the plan's Risks section) is two calls: keep the existing plain-`String` stream
    for the answer text, add a separate short structured call for `followUpQuestion`/`chips`/`tags`
    once the answer completes — either shape is acceptable for this task as long as the resulting
    `AskResponse`-shaped output is obtainable from one call to this new surface (from the caller's
    perspective) and the finding is documented.
  - **This task is additive, not a breaking replacement, to the existing plain-`String`
    `send(prompt:)`/`streamResponse(prompt:)`.** Despite the backlog item's "Replace the
    plain-`String` model-call surface" framing, the actual UI consumer of the new structured output
    (follow-up chips, message history) is Stage B item 9, explicitly deferred — so this task must
    **not** remove, rename, or change the existing `send`/`streamResponse` methods' signatures, and
    must **not** touch `Apin/Features/Ask/AskAndSaveServicing.swift`, `AskAndSaveService.swift`,
    `AskViewModel.swift`, `AskView.swift`, or `AskPhase`'s shape at all. Those files' current
    plain-string streaming contract stays completely unchanged this cycle. (T4 is the one task that
    reaches into the save path to *consume* this new structured surface — see T4's Scope — and even
    T4 does so additively, not by replacing the existing stream.)
  - `AssistantSessionService.mapGenerationError`'s existing `LanguageModelSession.GenerationError`
    handling stays unchanged in shape, just reachable from the new generation entry point too (per
    the plan's own framing).
  - Verify the existing system instructions (`PersonalitySystemInstructionBuilder`,
    `PersonalityBrief.placeholder`) read naturally as guidance for a structured multi-field response
    — per the plan's Open Question 5, adjust the new method's per-field `@Guide` descriptions if
    needed, but do **not** change `PersonalityBrief.swift`'s content itself (out of scope).
- **Dependencies:** none.
- **Acceptance criteria:**
  - `AssistantSessionService`'s new structured surface is covered by unit tests using a fake
    session (extend `AssistantSessionServiceTests.swift`/`FakeLanguageModelSession.swift`, per
    `memory/coding-standards.md`'s testing section) — AI response *content* stays out of scope for
    automated tests, but the new method's structure/error-mapping is tested.
  - `mapGenerationError`'s existing behavior is unchanged in shape (same switch cases, same
    `AssistantResponseError.Kind` mapping) — confirm via the existing test suite passing unmodified
    in its assertions about that mapping.
  - The existing `send(prompt:)`/`streamResponse(prompt:)` methods are unchanged in signature and
    behavior; `AssistantSessionServiceTests.swift`'s pre-existing tests for those methods pass
    unmodified.
  - `Apin/Features/Ask/AskAndSaveServicing.swift`, `AskAndSaveService.swift`, `AskViewModel.swift`,
    `AskView.swift` are **not modified** by this task (confirm via `git diff --stat`) —
    `AskViewModelTests.swift`'s existing phase-transition coverage passes unmodified, untouched.
  - Completion notes explicitly state the finding on partial-`@Generable` streaming support
    (supported / not supported / inconclusive-with-reasoning), since Stage B item 9 will need it.
  - `swift build` / `swift test` clean in `ApinCore`.
- **Notes:** This is the deepest architectural change in Stage A. The task-runner should budget
  real investigation time for the guided-generation API surface (macro syntax, whether
  `LanguageModelSession` exposes a streaming-with-generating variant) before committing to an
  implementation shape — the plan is explicit this wasn't live-doc-verified during planning.
  `context7`/live Apple documentation should be consulted for current `FoundationModels` API syntax
  before writing the `@Generable` type, same rigor prior cycles used for the iOS-floor checks.

**Completion notes (2026-08-12):** Implemented additively exactly as scoped. New `AskResponse`
(`@Generable`, `ApinCore/Sources/AI/AskResponse.swift`) with `answer`/`followUpQuestion`/
`chips: [String]` (2-4)/`tags: [String]` (1-5), each with a `@Guide(description:)`; an explicit
`public init` was required since `@Generable`'s macro expansion does not synthesize a public
memberwise initializer for a `public` type (confirmed via `-dump-macro-expansions` against the
real framework). New `sendStructured(prompt:) async -> Result<AskResponse, AssistantResponseError>`
added to `AssistantSessionService`, `respondStructured(to:)` added to the
`LanguageModelSessionProviding` seam and its `FoundationModelsSessionAdapter` conformance, mirrored
on `Apin/Features/Ask/AssistantSessionProviding.swift`. `send`/`streamResponse`/
`mapGenerationError` unchanged in shape; `AskAndSaveServicing.swift`, `AskAndSaveService.swift`,
`AskViewModel.swift`, `AskView.swift`, `PersonalityBrief.swift`, `Apin/Features/Journal/*`
untouched (confirmed via `git status --porcelain`). 5 new tests added to
`AssistantSessionServiceTests.swift` (+ fake-session support), 103/103 `ApinCore` tests pass,
`swiftlint` 0 violations.

API surface verified against two real, current sources (not training-data recall): Apple's
bundled `FoundationModels-Using-on-device-LLM-in-your-app.md` doc inside the installed Xcode
26.6, and the real `FoundationModels.framework`'s `.swiftinterface`, cross-checked via a
throwaway SPM scratch package that was built and deleted.

**Partial-`@Generable` streaming finding: SUPPORTED**, resolving the plan's open Risk item —
`LanguageModelSession.streamResponse<Content>(to:generating:...) -> ResponseStream<Content>`
exists and yields snapshots of `Content.PartiallyGenerated` (a macro-synthesized, all-optional
mirror type), confirmed directly in the dumped macro expansion. A future task building a
streaming structured-response UI can stream `AskResponse` directly — the two-call fallback the
plan flagged as a contingency is not needed. This task did not add a structured-streaming method
itself (out of scope here, UI consumption is a later cycle).

Flagged, not acted on (outside this task's scope): pre-existing stray `Apin 2.xcodeproj`/
`Apin 3.xcodeproj` duplicates at the repo root (known iCloud-Drive hazard, unrelated to this
task) — orchestrator's post-wave stray-duplicate sweep should still check for these. Full
`xcodebuild` for the `Apin` scheme was not run (ambiguous canonical `.xcodeproj` among the 3
copies); the one app-target file touched (`AssistantSessionProviding.swift`) was verified via
`swiftc -typecheck` against the built `ApinCore` module instead, cleanly.

### T3 — Excerpt derivation helper (first-sentence, non-stored)
- **Backlog item:** #5 (`planning/engineering-plan.md`).
- **Scope:**
  - New file under `ApinCore/Sources/Persistence/` (e.g. `JournalExcerpt.swift`), mirroring
    `JournalDigest.swift`'s pattern exactly: a plain, non-SwiftUI-importing, testable value
    type/static function that derives a first-sentence excerpt from a `JournalEntry.answer` string
    at render/query time.
  - **No `JournalEntry` schema change** — do not add a stored `@Model` property, do not touch
    `ApinCore/Sources/Persistence/JournalEntry.swift` at all. The excerpt is computed on demand,
    never persisted (per the plan's explicit Architecture Decision: "No `JournalEntry`
    schema/migration change").
  - Does not touch `JournalRepository`/`JournalQuery`/`SwiftDataJournalRepository` — no interface
    change needed, this is a pure helper function callers (Stage B's Journal row recreation, later)
    will call directly on an already-fetched `JournalEntry.answer`.
  - Does not touch any `Features/*` view — no current view renders an excerpt yet (Stage B's job).
- **Dependencies:** none.
- **Acceptance criteria:**
  - No new stored `@Model` property added to `JournalEntry.swift` (confirm via `git diff --stat` —
    that file is untouched).
  - New unit tests (same convention as `JournalDigestTests.swift`, e.g.
    `ApinCore/Tests/ApinCoreTests/JournalExcerptTests.swift`) cover: empty/blank `answer`, a
    single-sentence answer, a multi-sentence answer (excerpt stops at the first sentence
    boundary), and a sentence-less answer (no terminal punctuation — reasonable fallback behavior,
    task-runner's call, documented in the function's doc comment).
  - `swift build` / `swift test` clean in `ApinCore`.
- **Notes:** Small and fully independent — good candidate to run in whichever wave has spare
  capacity. "First sentence" should be defined precisely enough to be tested deterministically
  (e.g. split on `.`/`!`/`?` followed by whitespace or end-of-string) — document the exact rule
  chosen in the function's doc comment so Stage B's row-recreation task doesn't have to
  reverse-engineer it.

**Completion notes (2026-08-12):** Implemented as scoped —
`ApinCore/Sources/Persistence/JournalExcerpt.swift` (`JournalExcerpt.firstSentence(from:)`,
mirroring `JournalDigest.swift`'s pattern) plus `ApinCore/Tests/ApinCoreTests/
JournalExcerptTests.swift` (8 tests). Rule: trim whitespace/newlines, then find the first
`.`/`!`/`?` immediately followed by whitespace-or-end-of-string; excerpt runs through that
terminator. Documented limitation: no abbreviation detection (e.g. "e.g." followed by a space
still reads as a sentence boundary) — a deliberate simplicity trade-off, not an oversight.
Sentence-less input returns the full trimmed string as-is (no truncation); empty/blank input
returns `""`. `JournalEntry.swift`/`JournalRepository`/`JournalQuery`/
`SwiftDataJournalRepository`/`Features/*` all untouched (confirmed via `git diff --stat`).
`swift test` in `ApinCore`: 111/111 pass (8 new). Stray-duplicate sweep scoped to `ApinCore/`
found only gitignored `.build/` artifacts, no real duplicates.

### T4 — Auto-tags seeding on save
- **Backlog item:** #6 (`planning/engineering-plan.md`).
- **Scope:**
  - `Apin/Features/Ask/AskAndSaveServicing.swift` / `AskAndSaveService.swift`: the only call site
    that constructs and saves a `JournalEntry` (`AskAndSaveService.streamAndSave(prompt:)`,
    currently `JournalEntry(question: prompt, answer: latest)` with no `tags:` argument, defaulting
    to `[]`). Change this save step to seed `tags:` from T2's new structured-generation surface's
    `tags` output.
  - **Additive, not a replacement of the existing streaming UX:** do not remove or alter the
    existing plain-`String` streaming loop that `AskViewModel`/`AskPhase`/`AskView` depend on for
    the live-typing UI — that contract (`AsyncThrowingStream<String, Error>`, one cumulative string
    per emission) must stay exactly as it is. Obtain `tags` via an additional, separate call to
    T2's new structured entry point (e.g. once the streaming loop finishes and the final answer
    text is known, before/around the save), not by rewiring the primary stream itself. This is a
    deliberate, scoped extra on-device model call around save time — flag it explicitly in
    completion notes as a minor latency/battery cost, not silently absorbed.
  - `Apin/Features/Journal/JournalEntryDetailView.swift`'s existing manual tag add/edit UI is
    **unchanged and still available** — do not modify this file. Auto-generated tags become the
    save-time *default*, not the *only* mechanism; a user can still edit/clear/add tags afterward
    through the existing UI.
  - No `JournalRepository`/`JournalQuery` interface change (both already operate on plain
    `[String]` regardless of population source, per `memory/apis.md`'s `JournalQuery` entry) — do
    not touch `JournalRepository.swift`/`JournalQuery.swift`/`SwiftDataJournalRepository.swift`.
  - No `JournalEntry` schema change — `tags: [String]` already exists
    (`ApinCore/Sources/Persistence/JournalEntry.swift` line 40); do not touch that file.
- **Dependencies:** T2 (needs the structured `tags` output to exist before there's anything to
  seed with).
- **Acceptance criteria:**
  - `JournalEntryDetailView`'s existing manual tag-edit path still works unmodified (confirm via
    `git diff --stat` — that file untouched, and its existing tests, e.g.
    `ApinTests/JournalEntryDetailViewTests.swift`, pass unmodified).
  - A new unit test (extend `ApinTests/AskAndSaveServiceTests.swift` and/or
    `ApinTests/FakeAssistantSession.swift`) confirms: given a fake structured session returning a
    known `tags` array, the `JournalEntry` saved via `streamAndSave(prompt:)` has `tags` matching
    that array.
  - A new unit test confirms a failure/empty-tags path from the structured call degrades gracefully
    (saves with an empty `tags` array, does not fail the overall save/streaming flow) — the primary
    answer save must not become dependent on the tags call succeeding.
  - `AskViewModelTests.swift`'s existing phase-transition coverage passes unmodified — this task
    must not change `AskPhase`'s shape or `AskViewModel`'s public behavior.
  - `swift build` / `swift test` clean in both `ApinCore` and the `Apin` app target's test suites.
- **Notes:** This task starts `pending` but is not runnable until T2 reaches `done` — do not begin
  implementation against T2's *planned* shape from this document alone; read T2's actual
  completion notes/diff first, since the exact structured-call method signature is a
  during-implementation decision T2 makes (see T2's Notes on guided-generation API investigation).

**Completion notes (2026-08-12):** Implemented as scoped. `AskAndSaveService.swift`'s
`streamAndSave(prompt:)`: after the existing streaming loop finishes successfully (unchanged —
still forwards `assistantSession.streamResponse(prompt:)`'s cumulative-`String` values
unmodified), a new `private static func autoTags(from:prompt:)` helper calls
`assistantSession.sendStructured(prompt:)` and returns `.tags` on success, `[]` on failure;
result passed as `JournalEntry(question:answer:tags:)`'s new `tags:` argument. Gating: the
`#if canImport(FoundationModels)` check is localized to `autoTags` itself (returns `[]`
unconditionally outside the gate) rather than gating the whole file, mirroring
`AssistantSessionService.swift`'s own localized-gate pattern. `AskAndSaveServicing.swift`,
`JournalEntryDetailView.swift`, `JournalRepository.swift`, `JournalQuery.swift`,
`SwiftDataJournalRepository.swift`, `JournalEntry.swift`, `AskViewModel.swift`, `AskView.swift`
all untouched (confirmed via `git status --short`). Two new tests added to
`ApinTests/AskAndSaveServiceTests.swift` (tags-seeded-on-success, empty-tags-on-failure-without-
failing-the-stream), both `#if canImport(FoundationModels)`-gated. `swift build`/`swift test`
clean in `ApinCore`; `xcodebuild build-for-testing` for the `Apin` scheme succeeded
(`appintentsmetadataprocessor` completed cleanly for both `Apin` and `ApinTests` — compile-
verified per the sandbox proxy standard). Runtime test execution still blocked by the same
pre-existing App Group entitlement crash T5 already flagged — not attempted-and-passing, only
compile-verified; explicitly not overclaimed. `swiftlint`: 0 violations.

**Explicitly flagged design trade-off (for `/remember` to fold into `memory/decisions.md` or
`memory/technical-debt.md`):** every successful ask now makes a second, sequential on-device
model call (the `autoTags` structured call) after the primary answer stream finishes, before the
save resolves — added latency/battery cost per ask, deliberate and scoped per the plan's own
Architecture Decisions framing, not a defect.

### T5 — Root navigation composition (Ask ↔ Journal)
- **Backlog item:** #7 (`planning/engineering-plan.md`) / Architecture Decisions' "Root navigation
  composition owns reconciling `WeeklyDigestView`'s and `AskDeepLinkCoordinator`'s existing entry
  points" entry / `memory/decisions.md`'s 2026-08-12 "`WeeklyDigestView`'s entry point folds into
  the recreated Journal header" decision.
- **Scope:**
  - `Apin/ContentView.swift`: replace the current single `AskView(...)` composition (its own
    header comment already flags this as "NOT a final root-navigation composition") with a real
    Ask ↔ Journal composition: a journal button/affordance reachable from the Ask screen, showing a
    **live entry count** (`JournalRepository.fetchAll().count`, or an equivalent cheap live-updating
    count — e.g. a `@Query`-backed count in a small wrapper view), that navigates to
    `JournalListView`.
  - `Apin/Features/Ask/AskView.swift`: remove the `WeeklyDigestView` toolbar button, `
    isDigestPresented` state, and its `.sheet(...)` (currently lines ~48, 127-138) — per Kv's
    2026-08-12 decision, the digest entry point no longer lives on `AskView`'s toolbar. Do **not**
    delete `WeeklyDigestView` itself or its feature code (`Apin/Features/Digest/WeeklyDigestView.swift`
    is untouched) — only remove its current entry point from `AskView`.
  - The digest must remain reachable from *somewhere* in the app after this change — per the plan's
    explicit allowance, "a placeholder entry point is fine if Stage B hasn't landed yet." A minimal,
    even visually unstyled, toolbar button on the new root composition or on
    `Apin/Features/Journal/JournalListView.swift` that presents `WeeklyDigestView` as a sheet
    satisfies this. This task does **not** need to (and per the plan, should not try to) decide the
    digest's final visual placement near the week-strip — that is Stage B item 10's job, deferred.
  - `Apin/Features/Journal/JournalListView.swift`: this file's own header comment already
    anticipated this task ("Whichever task composes the app's real root navigation ... should
    decide whether to keep this stack or flatten it into a shared one") — reconcile
    `JournalListView`'s own `NavigationStack` with `AskView`'s own `NavigationStack` (currently both
    views independently wrap themselves in one) so the composed app doesn't nest two
    `NavigationStack`s. Exact navigation container/pattern (a shared `NavigationStack` with
    `navigationDestination`, a `TabView`, a push from Ask, etc.) is the task-runner's implementation
    call — Stage B's item 10 will pixel-match the final visual shape later; this task only needs a
    working, non-regressing composition.
  - **Must not regress `AskDeepLinkCoordinator`'s Spotlight/Siri deep-link prefill-and-submit flow**
    (`AskView.swift`'s `applyPendingDeepLinkQueryIfNeeded()`, `.onAppear`/`.onChange` wiring) — this
    needs a home in the new root composition; verify a deep-link-triggered prefill still reaches
    `AskView`'s `viewModel` and still auto-submits exactly as it does today.
  - Does not touch `AskViewModel.swift`, `AskAndSaveService.swift`, or any `ApinCore` file — this is
    purely a composition/wiring task in the app target.
- **Dependencies:** none.
- **Acceptance criteria:**
  - `ContentView.swift` no longer renders a bare `AskView(...)` as its entire body — a journal
    button with a live entry count, navigating to `JournalListView`, is reachable from the Ask
    screen.
  - `WeeklyDigestView` is still reachable via some toolbar/button entry point after this change
    (does not need to be visually polished or in its final Stage-B location).
  - `AskDeepLinkCoordinator`'s prefill-and-submit flow is verified still functionally unchanged —
    at minimum, existing coverage (`AskDeepLinkCoordinatorTests.swift` in `ApinCoreTests`, plus any
    `AskViewModelTests.swift`/`ApinTests` coverage exercising `prefill(with:)`) passes unmodified,
    and the task-runner manually traces (or, if feasible, adds) a test confirming a pending deep
    link set before `ContentView`'s body renders still reaches `AskView`'s view model.
  - No duplicate/nested `NavigationStack` bug (e.g. a broken back button, an unreachable toolbar)
    introduced by composing `AskView` and `JournalListView` together — confirm by building and
    exercising the flow in Simulator (screenshot or description in completion notes, same
    convention as prior cycles' `review/verification-assets/`).
  - `swift build`/`xcodebuild ... build` clean for the `Apin` scheme; `ApinTests` pass.
- **Notes:** This task is intentionally *not* asked to make the composition look like the handoff
  — only to make it structurally correct (Ask ↔ Journal reachable, digest reachable, deep-link
  flow intact). Resist the temptation to also apply T1's new design tokens here "since they're
  already available" — that blurs this task's scope with Stage B's and risks a half-styled,
  half-unstyled composition that's harder for `/review` to judge cleanly against either task's own
  acceptance criteria.

**Completion notes (2026-08-12):** Implemented as scoped: `ContentView.swift` now composes a
two-tab `TabView` (Ask default, Journal second) with a `@Query`-driven live entry-count `.badge`
on the Journal tab item. `AskView.swift`: removed `isDigestPresented`/toolbar button/`.sheet` and
the now-dead `journalRepository` property/init param (no remaining caller, confirmed via
repo-wide grep); `AskDeepLinkCoordinator` wiring untouched. `JournalListView.swift`: gained a
`journalRepository` property and a placeholder toolbar button presenting `WeeklyDigestView`
(final visual placement near the week-strip deferred to Stage B, per Kv's decision). Pattern
chosen: sibling `NavigationStack`s per tab (no nesting) — lowest-risk given the tight per-file
scope constraints. Deep-link flow verified by trace (no `selection:` binding on `TabView` means
Ask is the default/first tab, so `.onAppear` fires at launch exactly as before) plus
`AskDeepLinkCoordinatorTests` (6/6) passing unmodified. Full `Apin` scheme build (signing
disabled, see environment note) succeeded both for `generic/platform=iOS` and a real Simulator
destination. `swiftlint`: 0 violations on all 3 changed files.

**Gap surfaced, fixed by the orchestrator directly (outside T2's and T5's own scope):** T5's
task-runner found that T2's new `sendStructured(prompt:)` requirement on
`AssistantSessionProviding` left `ApinTests/FakeAssistantSession.swift` (an existing test double,
untouched by either T2 or T5 per their own scope constraints) non-conforming, blocking the full
`ApinTests` target from compiling. The orchestrator added a `structuredBehavior`/
`receivedStructuredPrompts` conformance to `FakeAssistantSession.swift` (same success/failure
shape as the existing `send`/`sendBehavior` pair, gated `#if canImport(FoundationModels)` to
match the protocol requirement) and re-verified: `ApinTests`' `appintentsmetadataprocessor` step
now completes with no compile errors (confirms the fix), full `Apin` scheme build still succeeds.
Full `ApinTests` runtime execution (`xcodebuild ... test`) still fails in this sandbox — but on
a `Fatal error: App Group container 'group.com.apin.app' is not reachable` at
`ApinApp.swift:107`, a pre-existing file untouched by any Cycle 5 task, caused by the same
missing-dev-team/entitlements environment gap T1 and T5 both already flagged (real code signing
needed for the App Group entitlement to resolve at runtime) — not a Cycle 5 regression.
`ApinWidgetTests` (no App-Group-dependent launch path) ran and passed 7/7 in the same invocation,
supporting that read. Flagging for `/qa`'s attention as a standing environment constraint: full
`ApinTests` execution needs a signed build (real dev team) in this sandbox, which is out of any
single task's scope to provide.

### T6 — Fast-follow: record Kv's execution of the Siri/Spotlight invocation re-check script
- **Backlog item:** #12 (`planning/engineering-plan.md`) — unchanged in substance from Cycle 4's
  backlog item 4 / `tasks/task-graph.md` (Cycle 4) T4, carried forward because Kv has still not
  executed/reported the script, not because the item resolved.
- **Scope:**
  - Once Kv has executed
    `review/manual-verification/siri-spotlight-invocation-recheck.md` (already produced, Cycle 4's
    T3) and reported results (via chat or by filling in the script's Result section directly),
    update:
    - `memory/decisions.md`: a new dated entry recording the actual outcome — whether Siri-voice
      invocation succeeded/failed, the actual state of the two Settings toggles, and how the result
      compares to Cycle 4 T2's build/metadata findings (`tasks/task-graph.md`'s Cycle 4 archive —
      note this file is about to be overwritten by this cycle's content, so if T6 executes after
      this graph is regenerated, source T2's findings from `memory/decisions.md`'s 2026-08-11
      entry, which already summarizes them, or from git history of the prior `tasks/task-graph.md`).
    - `memory/technical-debt.md` and/or spec open question #3, if warranted by the outcome
      (resolved with evidence, or re-confirmed still-open with a dated note).
  - No app code changes unless the result surfaces a genuine, clearly-scoped defect requiring one —
    if so, that's a new backlog item for a future cycle, not silently absorbed into T6.
- **Dependencies:** none expressible as a task-graph dependency this cycle (the script this task
  waits on already exists from Cycle 4's T3) — gated instead on Kv's actual device execution and
  report, which happens outside `/implement`'s reach.
- **Acceptance criteria:**
  - `memory/decisions.md`'s new entry reflects Kv's actual reported outcome, not an assumption or a
    guess.
  - If Kv has not reported by the time `/qa` runs this cycle, this task stays `blocked` and carries
    forward again — an acceptable, explicitly-tracked outcome per the plan's Definition of Done,
    not a failure to fix before `/qa`.
- **Notes:** Do not self-certify this task by reasoning about what Kv's device would "probably"
  show — a static-analysis argument does not satisfy this task, same discipline as Cycle 4's
  framing. This is now the fifth consecutive cycle touching this class of device-verification
  blocker (Cycles 3 and 4 both carried a version of this forward) — if T6 is still `blocked` when
  this cycle closes, that is worth surfacing to Kv explicitly (e.g. suggesting a standing,
  cycle-independent checklist instead of re-scoping it into every `/plan`), not quietly re-carrying
  it a sixth time.

## Blocked / notes

- **T6**: `blocked` from the start of this cycle — no code-level dependency, but gated on Kv
  executing `review/manual-verification/siri-spotlight-invocation-recheck.md` on the physical
  device and reporting back, which cannot happen inside an `/implement` session. Identical pattern
  to Cycle 4's T4. May or may not unblock within this cycle's boundary; either outcome is
  acceptable per the plan's Definition of Done.
- **T4**: not `blocked`, but not runnable as of this graph's generation — its one real dependency
  (T2) is still `pending`. Do not start T4 until T2 is `done` and its completion notes have been
  read (T2's exact structured-call shape is a during-implementation decision, not fully fixed by
  this document).
- **Stage B (backlog items 8-11) is out of scope for this graph, not merely deferred within it —
  no T-ids were generated for it.** See `planning/engineering-plan.md`'s Stage B table (items 8-11:
  Ask screen chrome, Ask screen message-history/follow-up chips, Journal screen recreation +
  digest placement, search/filter re-skin) for the scoped-but-deferred plan a follow-up cycle's
  `/tasks` run should pick up once Stage A has landed and been reviewed.
