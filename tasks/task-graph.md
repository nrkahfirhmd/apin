# Task Graph — Apin v1.2 (Cycle 3: personality content + human-in-the-loop device verification)

Generated by `/tasks` (the `planner` subagent) from `planning/engineering-plan.md`.
Each task is implemented independently by `/implement <task-id>` (the `task-runner` subagent).
Tasks with no unfinished dependency can run in parallel.

**Cycle 3 context:** this cycle is deliberately small — one content-only change and three
documentation deliverables (manual test scripts), plus one contingent follow-up. No task this
cycle touches shared files/interfaces with another task, so there are no real dependencies among
T1–T4. T5 is not a normal code task: its scope is to record Kv's reported results from executing
T2/T3/T4's scripts on the physical iPhone 17 outside this session, so it cannot start until Kv
reports back — it is listed as `blocked`, not `pending`, until that happens (may not close within
this cycle; see `planning/engineering-plan.md`'s Definition of Done and Risks).

## Tasks

| ID | Title | Depends on | Status | Owner agent |
|---|---|---|---|---|
| T1 | Personality brief content swap | — | done | task-runner |
| T2 | Manual verification script: zero network requests | — | done | task-runner |
| T3 | Manual verification script: Indonesian fluency check | — | done | task-runner |
| T4 | Manual verification script: Siri/Spotlight latency spike | — | done | task-runner |
| T5 | Fast-follow: record Kv's device-verification results | T2, T3, T4 | blocked | task-runner |

Status values: `pending → in-progress → in-review → done` (or `blocked` with a reason noted below).

T1–T4 are all dependency-free with respect to each other — T1 touches only
`ApinCore/Sources/AI/PersonalityBrief.swift`; T2–T4 each write one new, independent Markdown file
under `review/manual-verification/`. All four can run in the same wave, well within the
iCloud-Drive parallelism cap in `memory/coding-standards.md` (max 2 `task-runner` agents per
wave — so T1–T4 should run as two waves of 2, e.g. {T1, T2} then {T3, T4}, not all four at once).

T5 depends on T2–T4 existing (it can't record results against scripts that don't exist yet) *and*
on Kv actually executing them outside this session — the first dependency is real and blocks
`/implement T5` until T2–T4 are `done`; the second is not something `/implement` can satisfy at
all, so T5 stays `blocked` even after T2–T4 land, until Kv's report arrives.

Suggested wave ordering:
1. T1, T2 (independent files, Must-priority)
2. T3, T4 (independent files; T4 is Should-priority but no reason to sequence it after T3 beyond
   the parallelism cap)
3. T5 — not runnable this cycle unless Kv reports back on T2–T4 before `/qa` runs; otherwise
   carries forward, same as T4/T5/T10 did from cycle 2 (acceptable, tracked outcome).

## Task details

### T1 — Personality brief content swap
- **Backlog item:** #1 (`planning/engineering-plan.md`) / spec open question #2
- **Scope:**
  - `ApinCore/Sources/AI/PersonalityBrief.swift`: replace the placeholder brief content only —
    name framed as a pun on "Apple Intelligence," tone playful/cheerful, voice derived from Crow
    Armbrust (*Trails of Cold Steel*): witty, charming, mercenary-rogue energy, quips rather than
    lectures, cocky but likable, still answers directly (behavior-anchored, not a roleplay/persona
    simulation — see the plan's Risks section on overshooting into unwanted behavior).
  - Do **not** restructure `PersonalityBrief.swift`'s type shape or
    `PersonalitySystemInstructionBuilder.swift`'s building logic — content-only change.
  - Do not touch any other file.
- **Dependencies:** none
- **Acceptance criteria:**
  - `PersonalityBrief.swift`'s content reflects the confirmed brief (name-origin framing, tone,
    Crow-Armbrust-derived voice guidance).
  - `PersonalitySystemInstructionBuilderTests.swift` passes unmodified in shape (no test
    restructuring to accommodate the new content — if a test asserts on old placeholder wording
    specifically, update only the literal string being asserted against, not the test's structure).
  - `swift build` / `swift test` clean in `ApinCore`.
  - No other file changed.
- **Notes:** This is a content task, not a design task — the swappable structure already exists
  from cycle 1's T5. Keep the brief's guidance behavior-anchored per the plan's Risk framing: don't
  encode Crow Armbrust's full backstory/setting, just the tone/voice qualities (witty, quips over
  lectures, cocky-but-likable). AI response wording itself is out of scope for automated
  tests/assertions per `memory/coding-standards.md` — sanity-check actual model output manually
  during implementation if feasible, but that's not a blocking acceptance criterion.

### T2 — Manual verification script: zero network requests
- **Backlog item:** #2 (`planning/engineering-plan.md`) / `memory/technical-debt.md` "'Zero
  network requests...' not dynamically verified"
- **Scope:**
  - New file `review/manual-verification/zero-network-check.md`: a precise, self-contained
    checklist for Kv to execute on the connected iPhone 17 ("Kahfi's Phone") — exact steps to set
    up Instruments' Network instrument (or Network Link Conditioner set to "100% Loss"), exact
    steps to trigger a representative Ask flow, exact things to observe (any traffic captured
    during model invocation), exact pass/fail criteria, and a blank "Result" section for Kv to
    fill in (date, pass/fail, notes).
  - No app code changes.
- **Dependencies:** none
- **Acceptance criteria:**
  - Script is precise enough that Kv can execute it without re-reading `planning/engineering-plan.md`
    or asking a clarifying question first (per the plan's Risk mitigation).
  - Script references the existing static-analysis argument (`grep -rln "URLSession\|CFNetwork\|import Network"`
    across `Apin/`, `ApinCore/`, `ApinWidget/` returning zero matches) as context, not as a
    substitute for the dynamic check.
  - "Result" section exists and is blank (not pre-filled or guessed).
- **Notes:** Task is done when the script exists and is handed off — not when Kv has actually run
  it. Closing the underlying `memory/technical-debt.md` entry happens in T5, contingent on Kv's
  report.

### T3 — Manual verification script: Indonesian fluency check
- **Backlog item:** #3 (`planning/engineering-plan.md`) / spec open question #5 (technical half)
- **Scope:**
  - New file `review/manual-verification/indonesian-fluency-check.md`: a checklist with a set of
    representative Indonesian questions to ask Apin on-device, what "fluent/usable" output looks
    like vs. not, exact pass/fail criteria, explicit instruction that a fail means surfacing back
    to Kv for a scope decision (not auto-shrinking to English-only), and a blank "Result" section.
  - No app code changes.
- **Dependencies:** none
- **Acceptance criteria:**
  - Script includes at least 3–5 representative Indonesian questions covering different phrasing
    (a factual question, a conversational question, a longer/more complex question).
  - Pass/fail criteria are concrete (e.g. grammatically coherent, on-topic, no untranslated
    English fragments dropped in mid-sentence) rather than vague ("sounds good").
  - "Result" section exists and is blank.
- **Notes:** This checklist's outcome may also inform the still-open *product* question (spec open
  question #5's product half — should Apin mirror English/Indonesian at all) flagged in the plan's
  Open Questions — note that distinction in the script so Kv sees both angles when reporting back,
  but resolving the product question itself is not this task's job.

### T4 — Manual verification script: Siri/Spotlight latency spike
- **Backlog item:** #4 (`planning/engineering-plan.md`) / spec Req 5, open question #3 remainder
- **Scope:**
  - New file `review/manual-verification/siri-spotlight-latency-spike.md`: a checklist for
    measuring real on-device inference latency for a representative question via the existing
    Spotlight/Siri deep-link path (T13, shipped), what budget to compare it against (Apple's App
    Intent execution time budget — script should note where to find the current documented figure
    if not already known), exact go/no-go pass/fail criteria, explicit note that T13's deep-link
    remains the fallback regardless of outcome (so this is a spike, not a release blocker), and a
    blank "Result" section.
  - No app code changes.
- **Dependencies:** none
- **Acceptance criteria:**
  - Script specifies exactly how to time the measurement (e.g. stopwatch from Siri/Spotlight
    invocation to answer display, or an Instruments-based timing method if more precise and still
    feasible for Kv to run solo).
  - Go/no-go threshold is a concrete number or a concrete way to derive one, not left vague.
  - "Result" section exists and is blank.
- **Notes:** Should-priority spike — if this task can't fit in a wave with T1–T3 due to the
  parallelism cap, it can safely run last among T1–T4 without blocking anything else.

### T5 — Fast-follow: record Kv's device-verification results
- **Backlog item:** #5 (`planning/engineering-plan.md`)
- **Scope:**
  - Once Kv has executed T2/T3/T4's scripts and reported results (via chat or by filling in each
    script's "Result" section directly), update:
    - `memory/technical-debt.md`'s "'Zero network requests...' not dynamically verified" entry
      (resolved with evidence, or re-confirmed still-open with a dated note if the result was a
      fail or Kv couldn't complete it).
    - `memory/decisions.md`: new entries for the Indonesian-fluency result and the
      Siri/Spotlight-latency-spike go/no-go outcome.
  - No app code changes unless a result surfaces a genuine defect requiring one — if so, that's a
    new backlog item for a future cycle, not silently absorbed into T5.
- **Dependencies:** T2, T3, T4 (scripts must exist first) — plus, not expressible as a task-graph
  dependency, Kv's actual execution and report, which happens outside this session.
- **Acceptance criteria:**
  - Each of the three memory entries reflects Kv's actual reported outcome, not an assumption or
    a guess.
  - If Kv has not reported by the time `/qa` runs this cycle, this task stays `blocked` and
    carries forward — that is an acceptable, explicitly-tracked outcome per the plan's Definition
    of Done, not a failure to fix before `/qa`.
- **Notes:** Do not self-certify this task by running the scripts' *reasoning* without Kv's actual
  device execution — a static-analysis argument or a guess at what Indonesian output "probably"
  looks like does not satisfy this task, per the plan's explicit framing that items 2–4 are
  human-in-the-loop, not task-runner-executable.

## Blocked / notes

- **T5** is blocked pending Kv's execution of T2–T4's manual scripts on the physical iPhone 17 and
  a reported result. This may not resolve within this cycle — see `planning/engineering-plan.md`'s
  Risks section (three consecutive cycles of this same class of blocker: cycle 1 lacked hardware,
  cycle 2 lacked device-driving tooling, cycle 3 depends on Kv's own available time). If it
  recurs into cycle 4, the plan's Open Questions flags a process question worth Kv's input: should
  these verification items move out of the sprint-planning loop into a standing, cycle-independent
  checklist instead.
